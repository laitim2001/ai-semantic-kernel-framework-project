# Agent Swarm 可視化功能指南

## 功能介紹

Agent Swarm 可視化功能讓您能夠實時監控多代理協作任務的執行狀態。當系統需要多個 AI Agent 協同工作來完成複雜任務時，這個功能會自動顯示，讓您清楚了解每個 Agent 的工作進度。

### 主要功能

- **實時進度追蹤** - 查看整體任務進度和每個 Agent 的工作狀態
- **詳細執行資訊** - 深入了解每個 Agent 的思考過程和工具調用
- **Extended Thinking 展示** - 觀看 AI 的深度思考過程
- **工具調用記錄** - 查看 Agent 使用了哪些工具及其結果

## 介面說明

### Agent Swarm Panel（代理群組面板）

當多代理任務啟動時，Agent Swarm Panel 會自動出現在聊天介面的右側。

```
┌─────────────────────────────────────────┐
│  Agent Swarm                       [−]  │
├─────────────────────────────────────────┤
│  Overall Progress                       │
│  ████████████░░░░░░░░  65%              │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 🔍 DiagnosticWorker      ✅    │    │
│  │    Progress: 100%              │    │
│  │    Completed                   │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 📊 AnalysisWorker        🔄    │    │
│  │    Progress: 65%               │    │
│  │    Analyzing ETL Pipeline...   │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 🛠️ RemediationWorker    ⏳    │    │
│  │    Progress: 0%                │    │
│  │    Pending                     │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

#### 面板元素說明

| 元素 | 說明 |
|------|------|
| **Overall Progress** | 整體任務進度百分比 |
| **Worker Card** | 每個 Agent 的狀態卡片 |
| **狀態圖標** | ✅ 完成, 🔄 運行中, ⏳ 等待中, ❌ 失敗 |
| **Progress Bar** | 個別 Agent 的進度條 |
| **Current Task** | Agent 目前正在執行的任務 |

### Worker Card（Agent 卡片）

每張卡片顯示一個 Agent 的基本資訊：

- **Agent 名稱** - 如 DiagnosticWorker, AnalysisWorker
- **角色** - Agent 在任務中的職責
- **進度** - 工作完成百分比
- **狀態** - 目前的執行狀態
- **當前任務** - 正在進行的工作描述

### Worker Detail Drawer（Agent 詳情面板）

點擊任何一張 Worker Card 會開啟詳情面板，顯示該 Agent 的完整執行資訊。

```
┌─────────────────────────────────────────┐
│  ← AnalysisWorker                   ✕  │
├─────────────────────────────────────────┤
│  Status: Running                        │
│  Role: analyzer                         │
│  Progress: 65%                          │
│  Current Task: Analyzing ETL Pipeline   │
├─────────────────────────────────────────┤
│  Extended Thinking                      │
│  ┌─────────────────────────────────┐    │
│  │ 💭 I need to analyze the ETL   │    │
│  │    pipeline error logs to      │    │
│  │    identify patterns...        │    │
│  │    (245 tokens)                │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │ 💭 Found 47 connection timeout │    │
│  │    errors in the APAC region...│    │
│  │    (312 tokens)                │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  Tool Calls                             │
│  ┌─────────────────────────────────┐    │
│  │ 🔧 azure:query_adf_logs    ✅  │    │
│  │    Duration: 1.2s              │    │
│  │    ▶ Show Details              │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │ 🔧 database:query          🔄  │    │
│  │    Running...                  │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  Messages                               │
│  ┌─────────────────────────────────┐    │
│  │ 🤖 I will analyze the          │    │
│  │    pipeline errors...          │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

#### 詳情面板區塊

| 區塊 | 說明 |
|------|------|
| **Header** | Agent 名稱、狀態和基本資訊 |
| **Extended Thinking** | AI 的深度思考過程，顯示推理邏輯 |
| **Tool Calls** | Agent 調用的工具列表和執行結果 |
| **Messages** | Agent 產生的對話訊息 |

## 操作指南

### 查看任務進度

1. 當您發送需要多 Agent 處理的請求時，Agent Swarm Panel 會自動出現
2. 查看頂部的 Overall Progress 了解整體進度
3. 查看各個 Worker Card 了解每個 Agent 的狀態

### 查看 Agent 詳情

1. **點擊** 任何一張 Worker Card
2. Worker Detail Drawer 會從右側滑出
3. 查看 Extended Thinking 了解 AI 的思考過程
4. 展開 Tool Calls 查看工具調用詳情
5. 點擊右上角 **✕** 或面板外部關閉

### 展開/收起 Tool Call 詳情

1. 在 Worker Detail Drawer 中找到 Tool Calls 區塊
2. 點擊 **▶ Show Details** 展開工具調用詳情
3. 查看輸入參數和輸出結果
4. 點擊 **▼ Hide Details** 收起

### 收起 Agent Swarm Panel

1. 點擊面板標題右側的 **[−]** 按鈕
2. 面板會收起只顯示標題列
3. 點擊 **[+]** 重新展開

## 狀態說明

### Swarm 狀態

| 狀態 | 圖標 | 說明 |
|------|------|------|
| Initializing | 🔄 | 正在初始化多代理任務 |
| Running | 🔄 | 任務正在執行中 |
| Paused | ⏸️ | 任務暫停（等待人工審核） |
| Completed | ✅ | 所有 Agent 完成工作 |
| Failed | ❌ | 任務執行失敗 |

### Worker 狀態

| 狀態 | 圖標 | 說明 |
|------|------|------|
| Pending | ⏳ | 等待執行 |
| Running | 🔄 | 正在執行 |
| Thinking | 💭 | 正在深度思考 |
| Tool Calling | 🔧 | 正在調用工具 |
| Completed | ✅ | 執行完成 |
| Failed | ❌ | 執行失敗 |
| Cancelled | ⛔ | 已被取消 |

### Tool Call 狀態

| 狀態 | 圖標 | 說明 |
|------|------|------|
| Pending | ⏳ | 等待執行 |
| Running | 🔄 | 正在執行 |
| Completed | ✅ | 執行成功 |
| Failed | ❌ | 執行失敗 |

## 常見問題

### Q: Agent Swarm Panel 沒有出現？

**A:** Agent Swarm Panel 只在多代理任務啟動時自動顯示。單一 Agent 的任務不會觸發此功能。請嘗試發送需要多個 Agent 協作的複雜請求。

### Q: 進度條卡住不動？

**A:** 可能的原因：
1. Agent 正在進行長時間的工具調用
2. 正在等待外部 API 回應
3. 正在進行深度思考（Extended Thinking）

請查看 Worker Detail Drawer 了解當前執行狀態。

### Q: 如何查看 Agent 的思考過程？

**A:**
1. 點擊對應的 Worker Card
2. 在彈出的 Drawer 中找到 **Extended Thinking** 區塊
3. 每個思考區塊會顯示 AI 的推理內容和 token 數量

### Q: Tool Call 顯示失敗怎麼辦？

**A:**
1. 展開 Tool Call 詳情查看錯誤訊息
2. 錯誤可能是暫時性的網路問題
3. 系統通常會自動重試或使用替代方案
4. 如果問題持續，請聯繫系統管理員

### Q: 可以手動停止某個 Agent 嗎？

**A:** 目前系統會自動管理 Agent 的執行。如需停止整個任務，您可以：
1. 重新整理頁面
2. 發送新的請求覆蓋當前任務

### Q: 為什麼有些 Agent 顯示 Pending 很久？

**A:** 在 Sequential 模式下，Agent 會依序執行。後面的 Agent 需要等待前面的 Agent 完成才會開始。在 Parallel 模式下，所有 Agent 會同時開始。

### Q: 如何知道任務已經完成？

**A:**
1. Overall Progress 達到 100%
2. Swarm 狀態變為 ✅ Completed
3. 所有 Worker Card 都顯示 ✅ 完成狀態

## 鍵盤快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `Esc` | 關閉 Worker Detail Drawer |
| `Enter` | 選中的 Worker Card 上按下可開啟詳情 |
| `Tab` | 在 Worker Cards 間切換焦點 |

## 效能提示

1. **資料更新頻率** - 系統每 200ms 更新一次進度，確保流暢體驗
2. **長時間任務** - 對於耗時較長的任務，可以先處理其他工作，系統會持續追蹤
3. **多任務處理** - 每個對話 session 獨立追蹤 Swarm 狀態

---

**文件版本**: 1.0.0
**最後更新**: 2026-01-29
**適用版本**: IPA Platform Phase 29+
