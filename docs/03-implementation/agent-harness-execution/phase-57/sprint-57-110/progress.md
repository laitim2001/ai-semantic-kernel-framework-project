# Sprint 57.110 Progress — B4: subagent child governance + failure policies

[Plan](../../../agent-harness-planning/phase-57-frontend-saas/sprint-57-110-plan.md) · [Checklist](../../../agent-harness-planning/phase-57-frontend-saas/sprint-57-110-checklist.md)

---

## Day 0 — 2026-06-13 — Plan/Checklist + 三-prong verify + branch

**Done**: plan + checklist drafted (mirror 57.109 ten-section / Day 0-4 shape); 1 Explore recon agent (child-loop construction / parent governance wiring / failure semantics / HarnessPolicy / relay subset / design note 20) + direct greps; three-prong verify; branch `feature/sprint-57-110-subagent-child-governance` from `main` `220ed587`.

### Drift findings (Prong 1 + 2; Prong 3 N/A — no DB / no migration, harness_policy is 57.106 meta_data JSONB infrastructure)

- **D1 (🔴 load-bearing — engine composition is HITL-conditional)**: the C3 keyword escalate guardrails (input p5 / between-turns / output p5) register ONLY under `if hitl_manager is not None` (`handler.py:554-576`); `ToolGuardrail` (p10, :529-536) + `RiskyActionDetector` (p8, :545-549, default ON) register unconditionally after `build_default_guardrail_engine()` (:528). The chat 主流量 ALWAYS has `hitl_manager` → the composed engine the child inherits DOES carry the tenant phrases; ESCALATE-in-child then fail-closes per-gate (the child has no HITL wiring). Implication: child governance content tracks the parent's wiring — exactly the "same tenant policy, no second surface" design; documented, no scope change.
- **D2 (✓)**: the RiskyActionDetector registration comment (`handler.py:542-544`) PRE-ENDORSES the child semantic verbatim: "an ESCALATE without a deferred-pause manager fails closed to BLOCK in the loop — acceptable for a risky action".
- **D3 (✓)**: the output ESCALATE pre-delivery pause gate is entered ONLY with `is_final_answer` + full deferred-HITL wiring (`loop.py:1505-1506` docstring) — a child never enters it; no pause-attempt path exists in the child.
- **D4 (⚠️ behavior nuance — output ESCALATE is a child no-op)**: the per-response `_cat9_output_check` (`loop.py:1206-1236`) branches BLOCK (→ GUARDRAIL_BLOCKED terminate) and SANITIZE/REROLL (emit + continue) — ESCALATE has NO branch → falls through to continue. Effective child gates: INPUT (ESCALATE → soft-block, :673-680) · BETWEEN-TURNS (→ GUARDRAIL_BLOCKED, :1310-1311) · TOOL (:886 None-check → block path) · OUTPUT only for BLOCK-class actions (Toxicity/SensitiveInfo). dt leg A uses an INPUT phrase (unaffected); US-1 tests pin the per-gate reality incl. the output-ESCALATE no-op. Cross-ref plan §8 row "Child output gate never fires".
- **D5 (🔴 — budget construction + no LLM budget args)**: `budget=SubagentBudget()` is hardcoded in the task_spawn handler (`tools.py:119`); the ToolSpec input schema is `{task, mode}` ONLY — the LLM cannot set `max_duration_s` → the timeout dt lever does NOT exist. Plan §8 anticipated this: dt leg B reuses the SAME input-block child failure under `fail_fast`. Threading point = `make_task_spawn_tool` gains the failure-policy param and constructs `SubagentBudget(failure_policy=...)`.
- **D6 (✓ — FATAL mirror pinned)**: `RateLimitExceededError` (`_contracts/errors.py:111`) → `error_handling/policy.py:118` `self.register(RateLimitExceededError, ErrorClass.FATAL)` → `tools/executor.py:282` comment confirms the executor resolves it FATAL (no retry). `SubagentFailureEscalation` mirrors: define in `_contracts/errors.py` + register FATAL at the policy.py:118 region + raise from the task_spawn handler after `wait_for` (:123).
- **D7 (drift vs plan §4 row 7)**: the threading surface is `business_domain/_register_all.py` `make_default_executor` (:169; task_spawn registered at :292-295 via opt-in `subagent_dispatcher` + `parent_session_id`), NOT dispatcher.py/_category_factories. New opt-in kwarg on `make_default_executor` → forwarded to `make_task_spawn_tool`. File list adjusts (+`_register_all.py`, −`dispatcher.py` unless the as_tool mirror lands there — see D10).
- **D8 (✓ + in-scope honest-label improvement)**: `_drive`'s nonlocal `final_answer` (fork.py:141-159) survives timeout/exception cancellation → FAIL_PARTIAL salvage is fork.py-local (truncate + keep `error`). ALSO: a child INPUT-block run yields `LoopCompleted(stop_reason="guardrail_blocked")` with `final_answer=""` → today it falls to `error="empty_response"` (:183-191) — a dishonest label for a governance block. US-1 captures the child's `stop_reason` in `_drive` (we already isinstance LoopCompleted for tokens) → truthful `error="child_guardrail_blocked"`; the dt leg A asserts it.
- **D9 (✓)**: `TeammateExecutor` catch sites mirror fork's exactly (teammate.py:139 / :180-207 incl. `empty_response`) — the failure-policy + truthful-error edits mirror 1:1.
- **D10 (note)**: AS_TOOL rides ForkExecutor (salvage + truthful-error inherit free); the fail_fast raise is handler-level — the as_tool wrapper handler lives at `dispatcher.as_tool_factory` (:318). Mirror it if trivial at implementation; else record `AD-Subagent-AsTool-FailFast` (single-line decision, Day 2).
- **D11 (✓ FE pins — zero codegen)**: `guardrail_triggered` is an EXISTING wire type (generated events.json/loopEvents.generated.ts — no codegen change; the inner rides the `subagent_child` wrapper). `chatStore.ts:625` already has the parent-level `guardrail_triggered` case (rawEvents-only per :22); the `subagent_child` reducer is generic over inner (57.103 D2 precedent) — expected store delta ≈ projection of guardrail fields into the child-row text. `InspectorTree.tsx:139` `childTurnLabel` switch + `:153` `message_injected` = the mirror point; `ChildTurnEvent.kind` is a plain string (`features/subagent/types.ts:69`) → additive, no union edit.
- **D12 (✓ — threading order holds)**: parent executor built at `handler.py:444` with the opt-in deps; `policy` resolved at `:427` BEFORE it → the tenant failure policy is in scope at the call site. Child executors (:347 FORK / :378 TEAMMATE) are built WITHOUT `subagent_dispatcher` (recursion-safe — no task_spawn in children) → the failure-policy kwarg goes on the PARENT call only.
- **D13 (✓ — PUT seam pinned)**: `api/v1/admin/tenants.py:1625` `HarnessPolicyUpsertRequest` + the literal-check-422 pattern at `:1674-1679` (`verification_mode not in _VERIFICATION_MODES` → HTTPException 422) + the persisted dict at `:1765` + the GET response model (:1656 region). `subagent_failure_policy` mirrors all four spots.
- **Prong 1 (paths)**: all EDIT files Glob-1 (handler.py / _contracts/subagent.py / fork.py / teammate.py / tools.py / _register_all.py / harness_policy.py / admin tenants.py / chatStore.ts / InspectorTree.tsx / subagent types.ts / 17.md / design note 20); test APPEND targets exist (`test_harness_policy.py` unit/platform_layer/governance / test_fork.py / test_subagent_tools.py / test_subagent_child_loop.py / relay test); NEW basename `test_chat_subagent_governance.py` is UNIQUE across the whole test tree (57.109 D-DAY1-2 basename pre-check applied — 2nd deliberate application).
- **Baselines (recorded; verified at Day 1/3 gates)**: full pytest 2485 (2477+4skip non-e2e + 8 e2e) · wire count 24 · Vitest 836 · mockup-fidelity 51. (The recon's calibration-log "2283" anchor is stale — superseded by the 57.109 closeout figures.)

### Go/no-go

**GO** — D5/D7 move the threading to `_register_all.py` + `make_task_spawn_tool` (file-list adjustment, <10% scope); D4 narrows the child output-gate semantics (test-pinned; dt unaffected — leg A is input-phrase); D8 adds a small in-scope honest-label fix (`child_guardrail_blocked`). No plan §Technical-Spec rewrite (findings recorded here per AP-2 audit-trail rule; plan §8 rows already anticipated D4/D5 shapes).

---

## Day 1 — 2026-06-13 — US-1 child engine injection ✅

**Done**: `handler.py` both child factories inject `guardrail_engine` (late-bound closure — the composed engine is assembled after the factory defs; safe because factories only run at spawn time) + fork/teammate `_drive` captures the child `LoopCompleted.stop_reason` → truthful `error="child_guardrail_blocked"` (D8). Tests +5: helper engine-threading identity · input-ESCALATE fail-closes via ForkExecutor (`child_guardrail_blocked`) · direct-drive event pins (no ApprovalRequested; `GuardrailTriggered(input, action="escalate")` keeps the guardrail's truth while the RUN terminates `guardrail_blocked`) · tool-ESCALATE soft-blocks + child continues to its final answer · handler capture-and-delegate identity (FORK + TEAMMATE engines ARE `loop._guardrail_engine`).

**Drift findings (Day 1)**:
- **D-DAY1-1 (mypy deferred-analysis side effect)**: adding the late-bound `guardrail_engine` reference inside the closures made mypy DEFER + re-analyze `build_real_llm_handler`, which re-inferred the pre-existing conditional `subagent_dispatcher = make_chat_subagent_dispatcher(...) / else None` assignment strictly → a NEW error on an UNTOUCHED line (:433). Fix: explicit `"DefaultSubagentDispatcher | None"` annotation at the first assignment (+ TYPE_CHECKING import). Lesson: a closure capturing a later-assigned local can surface latent inference looseness elsewhere in the same function.
- **D-DAY1-2 (event-vs-run semantics pin)**: the input soft-block path emits `GuardrailTriggered(action="escalate")` — the EVENT keeps the guardrail's truthful action while the RUN fail-closes (`stop_reason="guardrail_blocked"`). The plan's "GuardrailTriggered(input, block)" wording (from the loop docstring) describes the no-identity branch; the keyword path preserves the original action. Test pins the real shape — better for US-2 visibility (the Tree row will truthfully say escalate).

**Gates**: subagent + handler suites 93 passed (+5, 0 del) · mypy strict 0/359 · flake8 0 · black/isort clean · `loop.py` UNTOUCHED.
