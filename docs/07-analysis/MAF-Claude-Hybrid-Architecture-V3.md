# 智能體編排平台：MAF + Claude Agent SDK 混合架構實現

> **文件版本**: 3.0
> **最後更新**: 2026-02-09
> **定位**: Agent Orchestration Platform (智能體編排平台)
> **狀態**: Phase 29 已完成 (106 Sprints, ~2400 Story Points)
> **代碼庫規模**: ~130,000+ LOC (Backend), ~180 .tsx/.ts (Frontend)
> **驗證方式**: 5 個專業分析 Agent 並行深度代碼庫逐一驗證

---

## 實現狀態總覽

> **重要說明**: 本文件是 V2 (Phase 28, 2026-01-28) 的全面更新。V3 反映 Phase 29 Agent Swarm Visualization 的完成，新增 `integrations/swarm/` 模組 (7 files, 2,747 LOC)、Swarm API (8 endpoints)、以及 16 個前端元件。同時基於 5 個分析 Agent 的深度驗證，修正了 V2 中多項低估的數據 (如 Stub 密度從 1.1% 修正為 6.6%、Mock 類嚴重度從「低」升級為「高」)。

### 各層實現狀態

| 層級 | 組件 | 檔案數 | LOC | 狀態 |
|------|------|--------|-----|------|
| **Layer 1** | Frontend | ~180 .tsx/.ts | 39 pages (25 routable), 102+ components | ✅ REAL |
| **Layer 2** | API Layer | 137 route files | ~534 endpoints, 41 routers | ✅ REAL |
| **Layer 3** | AG-UI Protocol | 23 files | ~9,531 | ✅ REAL |
| **Layer 4** | Phase 28 Orchestration | 39 files | ~15,753 | ✅ REAL - 含測試用 Mock |
| **Layer 5** | Hybrid Layer | 59 files | ~21,197 | ✅ REAL |
| **Layer 6** | MAF Builder Layer | 53 files | ~37,209 | ✅ REAL - 最成熟的整合層 |
| **Layer 7** | Claude SDK Layer | 46 files | ~15,098 | ✅ REAL |
| **Layer 8** | MCP Layer | 42 files | ~12,535 | ✅ REAL |
| **Layer 9** | Supporting + Swarm | 43 files | ~12,592 | ⚠️ 部分完成 (含 Swarm ✅) |
| **Layer 10** | Domain Layer | 112 files (19 modules) | ~40,000+ | ✅ REAL |
| **Layer 11** | Infrastructure | ~22 files | ~3,101 | ⚠️ RabbitMQ 為空殼 |
| | **總計** | **~630+ files** | **~130,000+ (Backend)** | |

### 已知問題

| # | 問題 | 影響 | 嚴重度 |
|---|------|------|--------|
| 1 | ContextSynchronizer 使用記憶體 dict，無 asyncio.Lock | 並行安全風險，競爭條件 | **高** |
| 2 | 單 Uvicorn Worker + `reload=True` 硬編碼 | 生產環境效能瓶頸 | **高** |
| 3 | Messaging/RabbitMQ 完全為空 (`__init__.py` only) | 無法使用訊息佇列 | **高** |
| 4 | 18 個 Mock 類存在於生產代碼中 | 代碼衛生、運行時風險 | **高** ↑ |
| 5 | 6 個 InMemory* 存儲預設在生產路徑 | 數據持久化風險 | **高** |
| 6 | ReactFlow 未安裝，但 `.claude/skills/react-flow/` 存在 | 工作流視覺化不可用 | 中 |
| 7 | Frontend 54 個 `console.log` | 效能與資訊洩漏 | 中 |
| 8 | 15 個檔案超過 800 行限制 | 可維護性下降 | 中 |
| 9 | `infrastructure/storage/` 為空目錄 | 缺少存儲抽象層 | 中 |
| 10 | Stub 密度 ~6.6% (V2 低估為 1.1%) | 功能完整度偏差 | 中 |
| 11 | 10 個過期 TODO 引用已完成的 Sprint | 代碼衛生 | 低 |

---

## 執行摘要

IPA Platform 是一個**智能體編排平台 (Agent Orchestration Platform)**，透過 Microsoft Agent Framework (MAF) 與 Claude Agent SDK 的混合架構，協調智能體集群處理需要**判斷力、專業知識與人機互動**的複雜任務。

平台核心理念：

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                      │
│   IPA Platform = Agent Orchestration Platform                        │
│   ═══════════════════════════════════════════                        │
│                                                                      │
│   「不是 n8n/Power Automate 的替代品，                               │
│     而是處理 n8n 無法處理的那些任務」                                │
│                                                                      │
│   MAF (Microsoft Agent Framework)                                    │
│   ─────────────────────────────────                                  │
│   • 結構化編排與治理                                                 │
│   • 多 Agent 協作模式 (Handoff, GroupChat, Magentic)                │
│   • 檢查點與狀態持久化                                               │
│   • 企業級審計追蹤                                                   │
│                                                                      │
│   Claude Agent SDK                                                   │
│   ─────────────────                                                  │
│   • 自主執行與深度推理                                               │
│   • Agentic Loop (自主迭代直到完成)                                  │
│   • Extended Thinking (複雜分析)                                     │
│   • 並行 Worker 執行                                                 │
│                                                                      │
│   AG-UI Protocol                                                     │
│   ──────────────                                                     │
│   • 即時 Agent 狀態串流                                              │
│   • Human-in-the-Loop 審批                                          │
│   • 思考過程可視化                                                   │
│   • Agent Swarm 群集即時視覺化 (V3 NEW)                             │
│                                                                      │
│   MCP (Model Context Protocol)                                       │
│   ─────────────────────────────                                      │
│   • 統一工具存取介面                                                 │
│   • 5 個 MCP Server (Azure, Shell, Filesystem, SSH, LDAP)           │
│   • 28 種權限模式、16 種審計模式                                     │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 關鍵數據

- **Backend**: ~130,000+ LOC across 630+ files (integration + domain + infrastructure)
- **API**: ~534 endpoints across 137 route files, 39 modules, 41 routers
- **Frontend**: ~180 .tsx/.ts files, 39 pages, 102+ components, 20 custom hooks, 3 stores
- **已完成**: 29 Phases, 106 Sprints, ~2400 Story Points
- **專案啟動**: 2025-11-14
- **技術棧**: FastAPI + React 18 + PostgreSQL + Redis + RabbitMQ (planned)
- **V3 新增**: Agent Swarm (7 backend files, 8 API endpoints, 16 frontend components)

---

## 1. 平台定位與價值主張

### 1.1 為什麼是「智能體編排平台」而非「工作流自動化」

V1 文件將本平台定位為「企業 IT 事件智能處理平台」，這個定位過於狹隘。實際上，平台的核心能力是**協調智能體集群**，適用於任何需要 AI 判斷力的複雜場景。

```
任務特徵分類：

┌─────────────────────────────────────────────────────────────────────┐
│                                                                      │
│   可預測、確定性任務                    不可預測、需判斷力的任務      │
│   ════════════════════                  ════════════════════════      │
│                                                                      │
│   「如果 X 則 Y」                       「分析情況，決定最佳方案」    │
│                                                                      │
│   • 新員工入職流程                      • 根因分析：為什麼系統掛了？ │
│   • 固定格式報表產出                    • 安全事件：這是否為攻擊？   │
│   • 定期資料備份                        • 資源規劃：如何分配預算？   │
│   • 審批路由 (固定規則)                 • 故障排除：哪裡出了問題？   │
│                                                                      │
│        ↓                                       ↓                     │
│   n8n / Power Automate                  IPA Platform                 │
│   (工作流自動化工具)                    (智能體編排平台)             │
│                                                                      │
│   特點：                                特點：                       │
│   • 視覺化流程設計                      • Agent 自主推理             │
│   • 確定性執行                          • 多 Agent 協作             │
│   • 固定邏輯分支                        • Human-in-the-Loop         │
│   • 無需 AI 判斷                        • 可觀測的決策過程           │
│   • 成本低、速度快                      • 適應性強、可學習           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 平台獨特價值

**場景一：使用者驅動的複雜任務**

```
使用者：「APAC Glider ETL Pipeline 連續第三天失敗，日報表完全無法產出」

IPA Platform 的處理方式 (非 n8n 可以做到的)：
─────────────────────────────────────────────
1. [意圖路由] 三層路由識別：IT 故障排查 + 高優先級
2. [引導對話] Agent 追問：哪個 Pipeline？錯誤訊息？影響範圍？
3. [風險評估] 判斷風險等級：涉及生產資料 → HIGH
4. [Agent 協作]
   ├─ 診斷 Agent → 查詢 Azure Data Factory 日誌
   ├─ 分析 Agent → Extended Thinking 分析根因
   └─ 修復 Agent → 提出修復方案
5. [HITL 審批] 生產環境變更 → 人工確認
6. [執行驗證] Agent 驗證修復結果、更新工單、通知團隊
7. [學習記錄] 記錄案例，未來類似問題加速處理
```

**場景二：Swarm 模式多 Agent 協作 (Phase 29 NEW)**

```
使用者：「幫我寫一份 Q1 技術策略報告」

IPA Platform Swarm 模式：
─────────────────────────
1. [Swarm 初始化] SwarmTracker 創建群集，分派 Worker
2. [並行執行]
   ├─ RESEARCH Worker → 蒐集市場資料
   ├─ ANALYST Worker  → 分析技術趨勢
   ├─ WRITER Worker   → 撰寫報告草稿
   └─ REVIEWER Worker → 審查品質
3. [即時可視化] SSE 串流 9 種事件到前端
4. [Swarm Panel] 前端即時顯示每個 Worker 的狀態、思考過程、工具調用
5. [結果彙總] Coordinator 整合所有 Worker 產出
```

**場景三：確定性 IT 運維 → n8n (不在本平台範疇)**

```
事件：新員工入職 → 建立帳號 → 分配權限 → 發送歡迎郵件

這是 n8n/Power Automate 的強項：
────────────────────────────────
• 固定步驟、固定邏輯
• 不需要 AI 判斷
• 成本更低、更可靠
• 本平台不與其競爭
```

### 1.3 核心價值定位

| 維度 | IPA Platform 提供的價值 | n8n 提供的價值 |
|------|------------------------|----------------|
| **智能判斷** | Agent 自主分析、推理、決策 | 無 (固定規則) |
| **多 Agent 協作** | Handoff, GroupChat, 並行 Worker, Swarm | 無 |
| **人機互動** | 即時審批、引導對話、思考可視化 | 固定審批節點 |
| **不確定性處理** | 適應性推理、動態策略調整 | 無 |
| **治理可見性** | 完整決策追蹤、審計日誌 | 基礎日誌 |
| **學習能力** | 案例學習、Few-shot 改進 | 無 |
| **Swarm 可視化** | 群集即時追蹤、Worker 狀態串流 | 無 |
| **確定性流程** | 非核心場景 | 核心強項 |
| **視覺化設計** | 開發中 | 成熟 |

---

## 2. 完整架構設計

### 2.0 端到端執行流程圖

> **重要**: 此圖展示完整的請求處理流程，從用戶輸入到結果返回的每一步驟和決策點。V3 新增 Swarm 執行路徑。

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                     IPA Platform 端到端執行流程圖 (Phase 29, V3)                      │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ╔═══════════════════════════════════════════════════════════════════════════════╗  │
│  ║                         入口層 + InputGateway                                 ║  │
│  ╠═══════════════════════════════════════════════════════════════════════════════╣  │
│  ║                                                                                ║  │
│  ║   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          ║  │
│  ║   │ ServiceNow  │  │   Teams     │  │ Prometheus  │  │  Chat UI    │          ║  │
│  ║   │  Webhook    │  │    Bot      │  │   Alert     │  │  (用戶輸入) │          ║  │
│  ║   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          ║  │
│  ║          │                │                │                │                  ║  │
│  ║          └────────────────┴────────────────┴────────────────┘                  ║  │
│  ║                                    │                                           ║  │
│  ║                                    ▼                                           ║  │
│  ║   ┌────────────────────────────────────────────────────────────────────────┐   ║  │
│  ║   │                         InputGateway                                   │   ║  │
│  ║   │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                │   ║  │
│  ║   │   │ ServiceNow   │  │  Prometheus  │  │  UserInput   │                │   ║  │
│  ║   │   │  Handler     │  │   Handler    │  │   Handler    │                │   ║  │
│  ║   │   └──────────────┘  └──────────────┘  └──────────────┘                │   ║  │
│  ║   │                        ↓ 來源識別 + 格式標準化                          │   ║  │
│  ║   │                   UnifiedRequestEnvelope                               │   ║  │
│  ║   └────────────────────────────────┬───────────────────────────────────────┘   ║  │
│  ╚════════════════════════════════════│═══════════════════════════════════════════╝  │
│                                       │                                              │
│  ╔════════════════════════════════════│═══════════════════════════════════════════╗  │
│  ║               Phase 28 三層意圖路由系統 (Orchestration Layer)                  ║  │
│  ╠════════════════════════════════════│═══════════════════════════════════════════╣  │
│  ║                                    ▼                                           ║  │
│  ║   ┌────────────────────────────────────────────────────────────────────────┐   ║  │
│  ║   │              BusinessIntentRouter (三層瀑布式路由)                      │   ║  │
│  ║   │                                                                        │   ║  │
│  ║   │   ┌──────────────────────────────────────────────────────────────┐    │   ║  │
│  ║   │   │ Layer 1: PatternMatcher (< 10ms)                             │    │   ║  │
│  ║   │   │ └─ 規則匹配 + 關鍵詞識別 → 置信度 > 0.9 直接輸出             │    │   ║  │
│  ║   │   │                    ↓ (未命中)                                 │    │   ║  │
│  ║   │   │ Layer 2: SemanticRouter (< 100ms) ★ V3: 真實 Azure OpenAI   │    │   ║  │
│  ║   │   │ └─ 向量相似度搜索 → 相似度 > 0.85 輸出                       │    │   ║  │
│  ║   │   │                    ↓ (未命中)                                 │    │   ║  │
│  ║   │   │ Layer 3: LLMClassifier (< 2000ms) ★ V3: 真實 Claude Haiku   │    │   ║  │
│  ║   │   │ └─ Claude Haiku 分類 + 完整度評估                            │    │   ║  │
│  ║   │   └──────────────────────────────────────────────────────────────┘    │   ║  │
│  ║   │                                │                                       │   ║  │
│  ║   │                   RoutingDecision {intent, completeness, risk}         │   ║  │
│  ║   └────────────────────────────────┬───────────────────────────────────────┘   ║  │
│  ║                                    │                                           ║  │
│  ║                    ┌───────────────┴───────────────┐                           ║  │
│  ║                    │      is_sufficient?           │                           ║  │
│  ║                    │      (資訊是否足夠?)          │                           ║  │
│  ║                    └───────────────┬───────────────┘                           ║  │
│  ║                          ┌─────────┴─────────┐                                 ║  │
│  ║                         Yes                  No                                ║  │
│  ║                          │                    │                                ║  │
│  ║                          │                    ▼                                ║  │
│  ║                          │   ┌──────────────────────────┐                      ║  │
│  ║                          │   │   GuidedDialogEngine     │                      ║  │
│  ║                          │   │  (引導式對話收集資訊)    │                      ║  │
│  ║                          │   └───────────┬──────────────┘                      ║  │
│  ║                          │               │ (補充後重新評估)                    ║  │
│  ║                          │   ←───────────┘                                     ║  │
│  ║                          ▼                                                     ║  │
│  ║   ┌──────────────────────────────────────────────────────────────────────┐     ║  │
│  ║   │                      RiskAssessor (風險評估)                         │     ║  │
│  ║   │   評估因素:                                                          │     ║  │
│  ║   │   • 受影響系統的關鍵性 (生產系統 +0.3)                               │     ║  │
│  ║   │   • 變更類型 (配置/數據變更 +0.2)                                    │     ║  │
│  ║   │   • 影響範圍 (>3 系統 +0.2)                                          │     ║  │
│  ║   │   • 事件優先級 (CRITICAL +0.1)                                       │     ║  │
│  ║   │   輸出: RiskLevel (LOW / MEDIUM / HIGH / CRITICAL)                   │     ║  │
│  ║   └────────────────────────────────┬─────────────────────────────────────┘     ║  │
│  ║                                    │                                           ║  │
│  ║                    ┌───────────────┴───────────────┐                           ║  │
│  ║                    │   需要人工審批?                │                           ║  │
│  ║                    │   (HIGH / CRITICAL 需審批)    │                           ║  │
│  ║                    └───────────────┬───────────────┘                           ║  │
│  ║                          ┌─────────┴─────────┐                                 ║  │
│  ║                         No                  Yes                                ║  │
│  ║                          │                    │                                ║  │
│  ║                          │                    ▼                                ║  │
│  ║                          │   ┌──────────────────────────────────────────────┐  ║  │
│  ║                          │   │            HITLController (人機協作)         │  ║  │
│  ║                          │   │  ┌─────────────┐  ┌─────────────┐            │  ║  │
│  ║                          │   │  │ Approval    │  │   Teams     │            │  ║  │
│  ║                          │   │  │ Handler     │  │ Notification│            │  ║  │
│  ║                          │   │  └─────────────┘  └─────────────┘            │  ║  │
│  ║                          │   │  等待審批... (timeout: 30 min)               │  ║  │
│  ║                          │   │  ↓ 超時升級到 IT Director                    │  ║  │
│  ║                          │   └───────────┬──────────────────────────────────┘  ║  │
│  ║                          │               │ (審批通過)                          ║  │
│  ║                          │   ←───────────┘                                     ║  │
│  ║                          ▼                                                     ║  │
│  ╚══════════════════════════│═════════════════════════════════════════════════════╝  │
│                             │                                                        │
│  ╔══════════════════════════│═════════════════════════════════════════════════════╗  │
│  ║                    MAF 編排層 + HybridOrchestratorV2                           ║  │
│  ╠══════════════════════════│═════════════════════════════════════════════════════╣  │
│  ║                          ▼                                                     ║  │
│  ║   ┌────────────────────────────────────────────────────────────────────────┐   ║  │
│  ║   │                 HybridOrchestratorV2 (中央協調器)                      │   ║  │
│  ║   │                                                                        │   ║  │
│  ║   │   FrameworkSelector 決策:                                              │   ║  │
│  ║   │   ┌────────────────────────────────────────────────────────────────┐  │   ║  │
│  ║   │   │ • 結構化任務 (已知模式) ────────────────→ MAF Builder          │  │   ║  │
│  ║   │   │ • 開放式推理 (Extended Thinking) ──────→ Claude SDK Worker     │  │   ║  │
│  ║   │   │ • 混合任務 ────────────────────────────→ MAF 編排 + Claude 執行│  │   ║  │
│  ║   │   │ • 多 Agent 群集 (V3 NEW) ─────────────→ Swarm Mode            │  │   ║  │
│  ║   │   └────────────────────────────────────────────────────────────────┘  │   ║  │
│  ║   │                                                                        │   ║  │
│  ║   │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                │   ║  │
│  ║   │   │ Checkpoint   │  │ Audit Logger │  │ AG-UI Server │                │   ║  │
│  ║   │   │ (狀態持久化) │  │ (審計日誌)   │  │ (SSE Stream) │                │   ║  │
│  ║   │   └──────────────┘  └──────────────┘  └──────────────┘                │   ║  │
│  ║   └────────────────────────────────┬───────────────────────────────────────┘   ║  │
│  ║                                    │                                           ║  │
│  ║                    ┌───────────────┴───────────────┐                           ║  │
│  ║                    │      Task Dispatcher          │                           ║  │
│  ║                    │   (任務分發到 Worker Pool)    │                           ║  │
│  ║                    └───────┬───────────┬───────────┘                           ║  │
│  ╚════════════════════════════│═══════════│═══════════════════════════════════════╝  │
│                               │           │                                          │
│              ┌────────────────┘           └────────────────┐                         │
│              ▼                                             ▼                         │
│  ╔═══════════════════════════════╗  ╔══════════════════════════════════════════╗    │
│  ║     Claude Worker 執行層      ║  ║        Swarm 群集執行層 (V3 NEW)         ║    │
│  ╠═══════════════════════════════╣  ╠══════════════════════════════════════════╣    │
│  ║  ┌─────────────────────────┐  ║  ║  ┌────────────────────────────────────┐  ║    │
│  ║  │     Worker Pool         │  ║  ║  │        SwarmTracker (693 LOC)      │  ║    │
│  ║  │  ┌──────┐ ┌──────┐     │  ║  ║  │  ┌──────────┐  ┌──────────┐       │  ║    │
│  ║  │  │Diag. │ │Remed.│     │  ║  ║  │  │ RESEARCH │  │ ANALYST  │       │  ║    │
│  ║  │  │Worker│ │Worker│ ... │  ║  ║  │  │  Worker  │  │  Worker  │  ...  │  ║    │
│  ║  │  └──────┘ └──────┘     │  ║  ║  │  └──────────┘  └──────────┘       │  ║    │
│  ║  └─────────────────────────┘  ║  ║  │  ┌──────────┐  ┌──────────┐       │  ║    │
│  ║                                ║  ║  │  │  WRITER  │  │ REVIEWER │       │  ║    │
│  ║                                ║  ║  │  │  Worker  │  │  Worker  │       │  ║    │
│  ║                                ║  ║  │  └──────────┘  └──────────┘       │  ║    │
│  ║                                ║  ║  └──────────────────┬───────────────┘  ║    │
│  ║                                ║  ║                      │                  ║    │
│  ║                                ║  ║    SwarmEventEmitter (634 LOC)          ║    │
│  ║                                ║  ║    9 種 SSE 事件即時串流                ║    │
│  ╚════════════════│═══════════════╝  ╚══════════════════════│═════════════════╝    │
│                   │                                         │                       │
│                   └──────────────────┬──────────────────────┘                       │
│                                      │                                              │
│  ╔═══════════════════════════════════│══════════════════════════════════════════╗  │
│  ║                           統一 MCP 工具層                                    ║  │
│  ╠═══════════════════════════════════│══════════════════════════════════════════╣  │
│  ║   ┌───────────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │                         MCP Gateway Service                           │  ║  │
│  ║   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌──────────┐    │  ║  │
│  ║   │   │   Azure     │  │   Shell     │  │ Filesystem  │  │SSH + LDAP│    │  ║  │
│  ║   │   │ MCP Server  │  │ MCP Server  │  │ MCP Server  │  │MCP Server│    │  ║  │
│  ║   │   └─────────────┘  └─────────────┘  └─────────────┘  └──────────┘    │  ║  │
│  ║   └───────────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════════════════════════════════════════════════════╝  │
│                                                                                      │
│  ╔══════════════════════════════════════════════════════════════════════════════╗  │
│  ║                    可觀測性層 + AG-UI 即時串流 (含 Swarm 事件)                ║  │
│  ╠══════════════════════════════════════════════════════════════════════════════╣  │
│  ║   AG-UI SSE 即時串流到前端:                                                   ║  │
│  ║   ┌────────────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │ • TEXT_MESSAGE_START/CONTENT/END  → 思考過程顯示                      │  ║  │
│  ║   │ • TOOL_CALL_START/END             → 工具調用顯示                      │  ║  │
│  ║   │ • APPROVAL_REQUEST                → 內聯審批卡片                      │  ║  │
│  ║   │ • STATE_SNAPSHOT/DELTA            → 共享狀態更新                      │  ║  │
│  ║   │ • STEP_STARTED/COMPLETED          → 進度追蹤                          │  ║  │
│  ║   │ • SWARM_CREATED/STATUS_UPDATE     → Swarm 群集狀態 (V3 NEW)          │  ║  │
│  ║   │ • WORKER_STARTED/PROGRESS/THINKING → Worker 即時追蹤 (V3 NEW)        │  ║  │
│  ║   │ • WORKER_TOOL_CALL/MESSAGE/COMPLETED → Worker 操作串流 (V3 NEW)      │  ║  │
│  ║   └────────────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════════════════════════════════════════════════════╝  │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘

執行流程摘要:
═══════════════
1. 用戶輸入 → InputGateway (來源標準化)
2. BusinessIntentRouter (三層瀑布式路由: Pattern → Semantic → LLM)
3. GuidedDialogEngine (若資訊不足，引導補充)
4. RiskAssessor (評估風險等級)
5. HITLController (高風險需審批)
6. HybridOrchestratorV2 (選擇 MAF / Claude / Swarm 框架)
7. Worker Pool 或 SwarmTracker (執行任務)
8. MCP Gateway (統一工具存取)
9. AG-UI SSE (即時串流結果到前端，含 Swarm 事件)
```

### 2.1 十一層架構總覽

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    IPA Platform：智能體編排平台架構 (V3)                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ╔═══════════════════════════════════════════════════════════════════════╗  │
│  ║  Layer 1: Frontend (使用者介面)                                       ║  │
│  ║  ~180 .tsx/.ts | 39 pages | 102+ components | 20 hooks | 3 stores   ║  │
│  ╠═══════════════════════════════════════════════════════════════════════╣  │
│  ║                                                                        ║  │
│  ║   React 18 + TypeScript + Vite (port 3005)                            ║  │
│  ║   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐             ║  │
│  ║   │ Unified  │  │  Agent   │  │   HITL   │  │  Swarm   │             ║  │
│  ║   │ Chat UI  │  │ Dashboard│  │ Approval │  │  Panel   │             ║  │
│  ║   │ (25+comp)│  │          │  │  Cards   │  │(16 comp) │             ║  │
│  ║   └──────────┘  └──────────┘  └──────────┘  └──────────┘             ║  │
│  ║   Stores: authStore | unifiedChatStore | swarmStore (NEW)             ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │ Fetch API                                │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 2: API Gateway (~534 endpoints, 137 route files, 41 routers) ║  │
│  ║  FastAPI (port 8000) + 39 API route modules + Swarm (8 endpoints)   ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 3: AG-UI Protocol (即時 Agent UI + Swarm SSE)                 ║  │
│  ║  23 files, ~9,531 LOC | AG-UI SSE Bridge + Swarm Event Streaming    ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 4: Input & Routing (輸入閘道 + 意圖路由)                      ║  │
│  ║  39 files, ~15,753 LOC | OrchestrationMetrics (893 LOC, OTel)       ║  │
│  ╠══════════════════════════════│════════════════════════════════════════╣  │
│  ║                              ▼                                        ║  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │  InputGateway            │   BusinessIntentRouter              │  ║  │
│  ║   │  (8 files, ~2,100 LOC)  │   (12 files, ~3,200 LOC)           │  ║  │
│  ║   │                          │                                     │  ║  │
│  ║   │  • ServiceNow Handler   │   • PatternMatcher   (< 10ms)      │  ║  │
│  ║   │  • Prometheus Handler   │   • SemanticRouter   (< 100ms) ★   │  ║  │
│  ║   │  • UserInput Handler    │   • LLMClassifier    (< 2000ms) ★  │  ║  │
│  ║   │  • Schema Validator     │   • CompletenessChecker             │  ║  │
│  ║   │                          │   ★ V3: 支援真實 LLM (CHANGE-003) │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │  GuidedDialog (5 files) │  RiskAssessor (3 files)             │  ║  │
│  ║   │  ~3,050 LOC             │  ~1,200 LOC                         │  ║  │
│  ║   │                          │                                     │  ║  │
│  ║   │  • Dialog Engine        │  • Risk Policies                    │  ║  │
│  ║   │  • Context Manager      │  • Assessment Engine                │  ║  │
│  ║   │  • Question Generator   │                                     │  ║  │
│  ║   │  • Refinement Rules     │  HITLController (4 files, ~1,900)   │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 5: Hybrid Orchestration (混合編排層)                           ║  │
│  ║  59 files, ~21,197 LOC                                                ║  │
│  ╠══════════════════════════════│════════════════════════════════════════╣  │
│  ║                              ▼                                        ║  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │            HybridOrchestratorV2 (1,254 LOC)                   │  ║  │
│  ║   │  中央協調器 - 整合所有 Phase 28/29 組件                        │  ║  │
│  ║   │                                                                │  ║  │
│  ║   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │  ║  │
│  ║   │  │ Framework    │  │ Context      │  │ Unified      │         │  ║  │
│  ║   │  │ Selector     │  │ Bridge       │  │ ToolExecutor │         │  ║  │
│  ║   │  │ (7 files)    │  │ (10 files)   │  │ (5 files)    │         │  ║  │
│  ║   │  └──────────────┘  └──────────────┘  └──────────────┘         │  ║  │
│  ║   │                                                                │  ║  │
│  ║   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │  ║  │
│  ║   │  │ Risk Engine  │  │ Switching    │  │ Checkpoint   │         │  ║  │
│  ║   │  │ (8 files)    │  │ Logic        │  │ Manager      │         │  ║  │
│  ║   │  └──────────────┘  └──────────────┘  └──────────────┘         │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 6: MAF Builder Layer (MAF 編排模式層)                          ║  │
│  ║  53 files, ~37,209 LOC — 22 builder files                            ║  │
│  ╠══════════════════════════════│════════════════════════════════════════╣  │
│  ║                              ▼                                        ║  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │  22 Builders (from agent_framework import ...)                │  ║  │
│  ║   │                                                                │  ║  │
│  ║   │  ┌────────────┐  ┌────────────┐  ┌────────────┐               │  ║  │
│  ║   │  │  Handoff   │  │ GroupChat  │  │ Concurrent │               │  ║  │
│  ║   │  │  Builder   │  │  Builder   │  │  Builder   │               │  ║  │
│  ║   │  └────────────┘  └────────────┘  └────────────┘               │  ║  │
│  ║   │  ┌────────────┐  ┌────────────┐  ┌────────────┐               │  ║  │
│  ║   │  │  Magentic  │  │  Planning  │  │  Nested    │               │  ║  │
│  ║   │  │  Builder   │  │  Builder   │  │ Workflow   │               │  ║  │
│  ║   │  └────────────┘  └────────────┘  └────────────┘               │  ║  │
│  ║   │  ┌────────────┐  ┌────────────┐  ┌────────────┐               │  ║  │
│  ║   │  │  Workflow   │  │   Agent   │  │   Code     │               │  ║  │
│  ║   │  │ Executor   │  │ Executor   │  │Interpreter │               │  ║  │
│  ║   │  └────────────┘  └────────────┘  └────────────┘               │  ║  │
│  ║   │  + handoff_hitl, handoff_policy, handoff_capability,          │  ║  │
│  ║   │    handoff_context, handoff_service, groupchat_voting,        │  ║  │
│  ║   │    groupchat_orchestrator, edge_routing, *_migration (5)      │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 7: Claude SDK Worker Layer (Claude 自主執行層)                 ║  │
│  ║  46 files, ~15,098 LOC                                                ║  │
│  ╠══════════════════════════════│════════════════════════════════════════╣  │
│  ║                              ▼                                        ║  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │  ClaudeSDKClient (AsyncAnthropic) - 171 LOC                   │  ║  │
│  ║   │                                                                │  ║  │
│  ║   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │  ║  │
│  ║   │  │ Autonomous   │  │ Hook System  │  │ Tool System  │         │  ║  │
│  ║   │  │ (7 files)    │  │ (6 files)    │  │ (6 files)    │         │  ║  │
│  ║   │  └──────────────┘  └──────────────┘  └──────────────┘         │  ║  │
│  ║   │                                                                │  ║  │
│  ║   │  ┌──────────────┐  ┌──────────────┐                           │  ║  │
│  ║   │  │ MCP Client   │  │ Orchestrator │                           │  ║  │
│  ║   │  │ (8 files)    │  │ (5 files)    │                           │  ║  │
│  ║   │  └──────────────┘  └──────────────┘                           │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════│════════════════════════════════════════╝  │
│                                 │                                          │
│  ╔══════════════════════════════│════════════════════════════════════════╗  │
│  ║  Layer 8: MCP Tool Layer (統一工具存取層)                             ║  │
│  ║  42 files, ~12,535 LOC                                                ║  │
│  ╠══════════════════════════════│════════════════════════════════════════╣  │
│  ║   ┌────────────────────────────────────────────────────────────────┐  ║  │
│  ║   │  5 MCP Servers + Core Protocol + Security                     │  ║  │
│  ║   │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │  ║  │
│  ║   │  │  Azure   │  │  Shell   │  │Filesystem│  │SSH + LDAP│      │  ║  │
│  ║   │  │ (9 files)│  │ (5 files)│  │ (5 files)│  │ (10 files)│     │  ║  │
│  ║   │  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │  ║  │
│  ║   │  core/ (5 files): Protocol, Transport, Client, Types         │  ║  │
│  ║   │  security/ (3 files): 28 permission patterns, 16 audit       │  ║  │
│  ║   └────────────────────────────────────────────────────────────────┘  ║  │
│  ╚══════════════════════════════════════════════════════════════════════╝  │
│                                                                              │
│  ╔══════════════════════════════════════════════════════════════════════╗  │
│  ║  Layer 9: Supporting Integrations + Swarm (支援整合層)               ║  │
│  ║  43 files, ~12,592 LOC | 含 Swarm (7 files, 2,747 LOC) ← V3 NEW  ║  │
│  ╚══════════════════════════════════════════════════════════════════════╝  │
│                                                                              │
│  ╔══════════════════════════════════════════════════════════════════════╗  │
│  ║  Layer 10: Domain Layer (業務邏輯層)                                  ║  │
│  ║  112 files, ~40,000+ LOC | 19 domain modules                       ║  │
│  ╚══════════════════════════════════════════════════════════════════════╝  │
│                                                                              │
│  ╔══════════════════════════════════════════════════════════════════════╗  │
│  ║  Layer 11: Infrastructure (基礎設施)                                  ║  │
│  ║  ~22 files, ~3,101 LOC | PostgreSQL 16 | Redis 7 | RabbitMQ (空殼) ║  │
│  ╚══════════════════════════════════════════════════════════════════════╝  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 資料流概覽

```
使用者輸入 / 系統事件
        │
        ▼
┌─────────────────┐
│  InputGateway   │ ← ServiceNow / Prometheus / UserInput Handler
│  (來源標準化)    │
└────────┬────────┘
         │ UnifiedRequestEnvelope
         ▼
┌─────────────────┐
│ BusinessIntent  │ ← PatternMatcher → SemanticRouter → LLMClassifier
│    Router       │    (三層瀑布式路由)
│  (意圖路由)     │    ★ V3: SemanticRouter 支援真實 Azure OpenAI embeddings
│                 │    ★ V3: LLMClassifier 支援真實 Claude Haiku
└────────┬────────┘
         │ RoutingDecision {intent, completeness, risk_hint}
         │
    ┌────┴────┐
    │ 資訊    │
    │ 足夠?   │
    └────┬────┘
    Yes  │  No → GuidedDialogEngine (引導式對話收集)
         │                │
         │       ←────────┘ (補充後重新評估)
         ▼
┌─────────────────┐
│  RiskAssessor   │ ← 風險政策評估
│  (風險評估)     │
└────────┬────────┘
         │ RiskLevel: LOW / MEDIUM / HIGH / CRITICAL
         │
    ┌────┴────┐
    │ 需要    │
    │ 審批?   │
    └────┬────┘
    No   │  Yes → HITLController → Teams Notification → 等待審批
         │                │
         │       ←────────┘ (審批通過)
         ▼
┌─────────────────────────┐
│ HybridOrchestratorV2    │ ← 中央協調器
│ ┌─────────────────────┐ │
│ │ FrameworkSelector   │ │ ← 選擇 MAF 模式 / Claude 自主 / Swarm
│ │ ContextBridge       │ │ ← 上下文同步
│ │ UnifiedToolExecutor │ │ ← 統一工具路由
│ │ CheckpointManager   │ │ ← 4 種存儲後端
│ └─────────────────────┘ │
└────────────┬────────────┘
             │
    ┌────────┼────────────────┐
    │        │                │
    ▼        ▼                ▼
┌─────────┐ ┌──────────┐ ┌──────────────┐
│   MAF   │ │  Claude  │ │   Swarm      │  ← V3 NEW
│ Builder │ │  SDK     │ │  Tracker     │
│ (9+種)  │ │ Worker   │ │ (9 Worker    │
│         │ │          │ │   Types)     │
└────┬────┘ └────┬─────┘ └──────┬───────┘
     │           │               │
     └─────┬─────┘               │
           │                     │
           ▼                     │
    ┌──────────────┐             │
    │  MCP Layer   │ ← 5 MCP Servers
    │  (工具執行)  │    28 permission patterns
    └──────┬───────┘
           │
           ▼                     │
    ┌──────────────┐             │
    │ AG-UI SSE    │ ←───────────┘  SwarmEventEmitter (9 事件類型)
    │ (結果回傳)   │
    └──────────────┘
```

---

## 3. 核心能力矩陣

### 3.1 Agent 編排能力

| 能力 | 實現方式 | 檔案位置 | 狀態 |
|------|----------|----------|------|
| **Sequential 執行** | MAF Sequential Builder | `integrations/agent_framework/builders/workflow_executor.py` | ✅ |
| **Handoff 交接** | MAF Handoff Builder + Policy + Context | `builders/handoff.py`, `handoff_policy.py`, `handoff_context.py` | ✅ |
| **GroupChat 討論** | MAF GroupChat Builder + Voting | `builders/groupchat.py`, `groupchat_voting.py` | ✅ |
| **Magentic 推理** | MAF Magentic Builder | `builders/magentic.py` | ✅ |
| **並行執行** | MAF Concurrent Builder | `builders/concurrent.py` | ✅ |
| **巢狀工作流** | MAF Nested Workflow | `builders/nested_workflow.py` | ✅ |
| **Planning 規劃** | MAF Planning Builder | `builders/planning.py` | ✅ |
| **自主執行** | Claude SDK Autonomous | `claude_sdk/autonomous/executor.py` | ✅ |
| **深度推理** | Claude Extended Thinking | `claude_sdk/autonomous/analyzer.py` | ✅ |
| **框架切換** | Switching Logic (4 triggers) | `hybrid/switching/` (9 files) | ✅ |

**框架切換觸發條件**:

```
FrameworkSwitcher (integrations/hybrid/switching/)
├── user_trigger.py       → 使用者明確要求切換
├── failure_trigger.py    → 當前框架執行失敗
├── resource_trigger.py   → 資源限制 (token, 時間)
└── complexity_trigger.py → 任務複雜度變化
```

### 3.2 Human-in-the-Loop 能力

| 能力 | 實現方式 | 檔案位置 | 狀態 |
|------|----------|----------|------|
| **審批請求** | HITLController + ApprovalHandler | `orchestration/hitl/controller.py`, `approval_handler.py` | ✅ |
| **Teams 通知** | Notification Service | `orchestration/hitl/notification.py` | ✅ |
| **即時審批 UI** | AG-UI HITL Components | `ag_ui/features/human_in_loop.py` | ✅ |
| **引導式對話** | GuidedDialogEngine | `orchestration/guided_dialog/engine.py` | ✅ |
| **前端審批卡片** | React HITL Components | `frontend/src/components/ag-ui/hitl/` | ✅ |
| **Claude Hooks** | Approval + Sandbox Hooks | `claude_sdk/hooks/approval.py`, `sandbox.py` | ✅ |

### 3.3 工具與資源存取能力

| 能力 | 實現方式 | 檔案位置 | 狀態 |
|------|----------|----------|------|
| **Azure 管理** | Azure MCP Server (5 modules) | `mcp/servers/azure/` (9 files) | ✅ |
| **Shell 執行** | Shell MCP Server | `mcp/servers/shell/` (5 files) | ✅ |
| **檔案系統** | Filesystem MCP Server | `mcp/servers/filesystem/` (5 files) | ✅ |
| **SSH 遠端** | SSH MCP Server | `mcp/servers/ssh/` (5 files) | ✅ |
| **LDAP 查詢** | LDAP MCP Server | `mcp/servers/ldap/` (5 files) | ✅ |
| **統一路由** | UnifiedToolExecutor | `hybrid/execution/` (5 files) | ✅ |
| **權限控制** | Security Module | `mcp/security/` (3 files, 28 patterns) | ✅ |
| **Code Interpreter** | MAF CodeInterpreter Builder | `builders/code_interpreter.py` | ✅ |

### 3.4 可觀測性與治理能力

| 能力 | 實現方式 | 檔案位置 | 狀態 |
|------|----------|----------|------|
| **路由指標** | OrchestrationMetrics | `orchestration/metrics.py` (893 LOC) | ✅ |
| **OpenTelemetry** | OTel with fallback counters | `orchestration/metrics.py` | ✅ |
| **巡檢系統** | Patrol Agent (5 check types) | `patrol/` (11 files, ~2,541 LOC) | ✅ |
| **審計追蹤** | Decision Tracker + Report | `audit/` (4 files, ~1,166 LOC) | ✅ |
| **風險評估** | Risk Engine + Scorers | `hybrid/risk/` (8 files) | ✅ |
| **Checkpoint** | 4 storage backends | `hybrid/checkpoint/` (9 files) | ✅ |

**Checkpoint 儲存後端**:

```python
# integrations/hybrid/checkpoint/ (9 files)
├── memory_store.py     → 記憶體 (開發用)
├── redis_store.py      → Redis (推薦生產用)
├── postgres_store.py   → PostgreSQL (持久化)
└── filesystem_store.py → 檔案系統 (備用)
```

### 3.5 記憶與學習能力

| 能力 | 實現方式 | 檔案位置 | 狀態 |
|------|----------|----------|------|
| **統一記憶** | mem0 Integration | `memory/` (5 files, ~1,508 LOC) | ✅ |
| **向量嵌入** | Embedding Service | `memory/embeddings.py` | ✅ |
| **Few-shot 學習** | Learning Module | `learning/` (5 files, ~1,236 LOC) | ⚠️ PARTIAL |
| **案例提取** | Case Extractor | `learning/case_extractor.py` | ⚠️ PARTIAL |
| **相似度匹配** | Similarity Engine | `learning/similarity.py` | ⚠️ PARTIAL |
| **關聯分析** | Correlation Graph | `correlation/` (4 files, ~1,188 LOC) | ⚠️ PARTIAL |

### 3.6 Agent Swarm 能力 (Phase 29 NEW)

Phase 29 (Sprint 100-106) 新增的多 Agent 群集即時視覺化系統，包含完整的後端追蹤、SSE 事件串流、API 端點與前端面板。

#### 3.6.1 後端 Swarm 模組

| 組件 | 檔案 | LOC | 職責 |
|------|------|-----|------|
| **SwarmTracker** | `swarm/tracker.py` | 693 | 群集生命週期管理、Worker 追蹤、狀態轉換 |
| **SwarmEventEmitter** | `swarm/events/emitter.py` | 634 | 9 種 SSE 事件類型的產生與分發 |
| **SwarmIntegration** | `swarm/swarm_integration.py` | 404 | AG-UI Bridge 整合、CustomEvent 包裝 |
| **Swarm Models** | `swarm/models.py` | ~300 | 資料模型定義 (Worker, Swarm, Event) |
| **Swarm Events** | `swarm/events/` | ~400 | 事件類型定義與序列化 |
| **Swarm __init__** | `swarm/__init__.py` | ~50 | 模組匯出 |

**Swarm 資料模型**:

```
WorkerType (9 enum):
├── RESEARCH     → 資料蒐集
├── WRITER       → 內容撰寫
├── DESIGNER     → 設計產出
├── REVIEWER     → 品質審查
├── COORDINATOR  → 協調管理
├── ANALYST      → 資料分析
├── CODER        → 代碼開發
├── TESTER       → 測試驗證
└── CUSTOM       → 自定義

WorkerStatus (7 lifecycle):
PENDING → RUNNING → THINKING → TOOL_CALLING → COMPLETED / FAILED / CANCELLED

SwarmMode (3 modes):
├── SEQUENTIAL    → 順序執行
├── PARALLEL      → 並行執行
└── HIERARCHICAL  → 階層執行

SwarmStatus (5 states):
INITIALIZING → RUNNING → PAUSED → COMPLETED / FAILED
```

**SwarmEventEmitter 9 種事件類型**:

```
Swarm 層級事件:
├── SwarmCreated        → 群集已建立
├── SwarmStatusUpdate   → 群集狀態變更
└── SwarmCompleted      → 群集執行完成

Worker 層級事件:
├── WorkerStarted       → Worker 開始執行
├── WorkerProgress      → Worker 進度更新
├── WorkerThinking      → Worker 思考過程 (Extended Thinking)
├── WorkerToolCall      → Worker 工具調用
├── WorkerMessage       → Worker 訊息產出
└── WorkerCompleted     → Worker 執行完成
```

#### 3.6.2 Swarm API 端點

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/v1/swarm/{swarm_id}` | 群集狀態查詢 |
| GET | `/api/v1/swarm/{swarm_id}/workers` | Worker 列表 |
| GET | `/api/v1/swarm/{swarm_id}/workers/{worker_id}` | Worker 詳情 |
| POST | `/api/v1/swarm/demo/start` | 啟動 Demo 群集 |
| GET | `/api/v1/swarm/demo/status/{swarm_id}` | Demo 狀態查詢 |
| POST | `/api/v1/swarm/demo/stop/{swarm_id}` | 停止 Demo |
| GET | `/api/v1/swarm/demo/scenarios` | 可用場景列表 |
| GET | `/api/v1/swarm/demo/events/{swarm_id}` | SSE 事件串流 |

#### 3.6.3 Swarm 前端面板

**16 個元件** (位於 `frontend/src/components/unified-chat/agent-swarm/`):

| 元件 | Sprint | 用途 |
|------|--------|------|
| AgentSwarmPanel | 102 | 主面板 (loading/empty/active 三態) |
| SwarmHeader | 102 | 模式和狀態顯示 |
| OverallProgress | 102 | 狀態感知進度條 |
| WorkerCard | 102 | 個別 Worker 狀態卡片 |
| WorkerCardList | 102 | Worker 卡片網格佈局 |
| SwarmStatusBadges | 102 | 狀態徽章集合 |
| WorkerDetailDrawer | 103 | 側邊抽屜 (Worker 詳情) |
| WorkerDetailHeader | 103 | 抽屜標題列 |
| CurrentTask | 103 | 當前任務顯示 |
| ToolCallItem | 103 | 工具調用視覺化 |
| ToolCallsPanel | 103 | 工具調用列表 |
| MessageHistory | 103 | Worker 對話歷史 |
| CheckpointPanel | 103 | Worker 檢查點 |
| ExtendedThinkingPanel | 104 | Extended Thinking 視覺化 |
| WorkerActionList | 104 | 統一操作時間線 |
| OrchestrationPanel | 105 | 編排控制面板整合 |

**4 個 Hooks + 1 個 Store**:

| Hook/Store | 用途 |
|------------|------|
| `useSwarmMock` | Mock 數據驅動 (開發/測試用) |
| `useSwarmReal` | 真實 SSE 串流驅動 |
| `useSwarmOrchestration` | 編排面板狀態管理 |
| `useSwarmWorkerDetail` | Worker 詳情管理 |
| `swarmStore` (Zustand) | 全域 Swarm 狀態 |

---

## 4. 技術棧實現詳情

### 4.1 Layer 1: Frontend (~180 .tsx/.ts files)

```
frontend/src/
├── pages/ (39 pages, 25 routable, 23 routes)
│   ├── agents, workflows, dashboard
│   ├── DevUI (7 開發者工具頁面)
│   ├── Settings, Auth
│   └── SwarmTestPage.tsx ← V3 NEW
├── components/ (102+ components)
│   ├── unified-chat/ (25+ components) ← 主聊天介面
│   ├── unified-chat/agent-swarm/ (16 components) ← V3 NEW
│   ├── ag-ui/ (Chat, HITL, Advanced)
│   ├── ui/ (Shadcn UI)
│   └── layout, shared, auth, DevUI
├── hooks/ (20 custom hooks)
│   ├── 16 top-level hooks
│   └── 4 internal swarm hooks ← V3 NEW
├── api/ (Fetch API, NOT Axios)
├── store/, stores/ (Zustand: authStore, unifiedChatStore, swarmStore)
└── types/, utils/
```

### 4.2 Layer 2: API Layer (~534 endpoints, 137 route files, 41 routers)

API 層橫跨 39 模組，提供完整的 RESTful 介面:

```
backend/src/api/v1/
├── agents, workflows, sessions, executions
├── ag_ui, claude_sdk, hybrid, mcp
├── orchestration, autonomous, routing
├── patrol, correlation, rootcause, audit
├── swarm/ (5 files, 8 endpoints) ← V3 NEW
│   ├── __init__.py
│   ├── routes.py       → 3 status endpoints
│   ├── demo.py         → 5 demo endpoints (含 SSE)
│   ├── schemas.py      → Request/Response schemas
│   └── dependencies.py → DI 依賴注入
└── auth, files, sandbox, checkpoints, etc.
```

### 4.3 Layer 3: AG-UI Protocol (23 files, ~9,531 LOC)

AG-UI (Agentic UI) 層實現了 Agent 狀態的即時前端可視化。V3 新增 Swarm 事件整合。

```python
# integrations/ag_ui/ (23 files, ~9,531 LOC)

features/
├── human_in_loop.py   # HITL 審批 UI 事件
├── generative_ui.py   # 動態 UI 生成
├── predictive_ui.py   # 預測性 UI 更新
└── shared_state.py    # Agent-Frontend 共享狀態

events/                # AG-UI 事件定義
handlers/              # 事件處理器
bridge/                # SSE Bridge (Server-Sent Events)
```

**SSE 串流機制 (V3 擴展)**:

```
Agent 執行過程
    │
    ├─ Thinking Event     → 前端顯示思考過程
    ├─ Tool Call Event    → 前端顯示工具調用
    ├─ HITL Event         → 前端顯示審批卡片
    ├─ Progress Event     → 前端更新進度條
    ├─ Result Event       → 前端顯示最終結果
    │
    │  V3 Swarm 事件 (透過 CustomEvent 橋接):
    ├─ Swarm Created      → 前端初始化 Swarm Panel
    ├─ Worker Started     → 前端新增 Worker Card
    ├─ Worker Thinking    → 前端顯示 Extended Thinking
    ├─ Worker Tool Call   → 前端顯示工具調用時間線
    ├─ Worker Progress    → 前端更新 Worker 進度
    └─ Swarm Completed    → 前端顯示完成摘要

    全部透過 SSE (Server-Sent Events) 即時推送
```

### 4.4 Layer 4: Phase 28 Orchestration (39 files, ~15,753 LOC)

Phase 28 (Sprint 91-99, 235 Story Points) 實現了三層意圖路由系統，是平台的智能入口。

**三層瀑布式路由**:

```
使用者輸入: "ETL Pipeline 又失敗了"
                │
                ▼
┌──────────────────────────────────────────────┐
│ Tier 1: PatternMatcher (< 10ms)              │
│ integrations/orchestration/intent_router/    │
│ pattern_matcher/matcher.py (358 LOC)         │
│                                              │
│ 規則匹配 + 關鍵詞識別                        │
│ "ETL" + "失敗" → IT_INCIDENT (confidence 0.92)│
│                                              │
│ if confidence > 0.9: return result ✅         │
│ else: fallthrough ↓                          │
└──────────────────────────────────────────────┘
                │ (如未命中)
                ▼
┌──────────────────────────────────────────────┐
│ Tier 2: SemanticRouter (< 100ms)             │
│ semantic_router/router.py (465 LOC)          │
│                                              │
│ ★ V3 CHANGE-003: 支援真實 Azure OpenAI      │
│   USE_REAL_ROUTER=true → Azure embeddings    │
│   無 API key → 自動降級至 Mock               │
│                                              │
│ if similarity > 0.85: return result ✅        │
│ else: fallthrough ↓                          │
└──────────────────────────────────────────────┘
                │ (如未命中)
                ▼
┌──────────────────────────────────────────────┐
│ Tier 3: LLMClassifier (< 2000ms)             │
│ llm_classifier/classifier.py (438 LOC)       │
│                                              │
│ ★ V3 CHANGE-003: 支援真實 Claude Haiku      │
│   USE_REAL_ROUTER=true → Claude Haiku 分類   │
│   無 API key → 自動降級至 Mock               │
│                                              │
│ return RoutingDecision ✅                     │
└──────────────────────────────────────────────┘
```

**引導式對話引擎**:

```python
# integrations/orchestration/guided_dialog/
# 5 files, ~3,050 LOC

engine.py           # GuidedDialogEngine - 核心對話引擎
context_manager.py  # 對話上下文管理
generator.py        # 問題生成器 (基於缺失資訊)
refinement_rules.py # 追問規則 (哪些資訊需要補充)
__init__.py
```

**風險評估器**:

```python
# integrations/orchestration/risk_assessor/
# 3 files, ~1,200 LOC

assessor.py   # 風險評估引擎
policies.py   # 風險政策定義 (操作類型 → 風險等級)
__init__.py
```

**HITL 控制器**:

```python
# integrations/orchestration/hitl/
# 4 files, ~1,900 LOC

controller.py        # HITL 控制器 (協調審批流程)
approval_handler.py  # 審批處理 (同意/拒絕/超時)
notification.py      # Teams 通知發送
__init__.py
```

**可觀測性指標**:

```python
# integrations/orchestration/metrics.py (893 LOC) ← V3 更新: V2 為 763 LOC
# OpenTelemetry with fallback counters

class OrchestrationMetrics:
    # 路由指標: 各層命中率、延遲
    # 對話指標: 對話輪次、完成率
    # 風險指標: 各風險等級分布
    # HITL 指標: 審批時間、通過率
```

### 4.5 Layer 5: Hybrid Layer (59 files, ~21,197 LOC)

混合層是整個平台的中央協調者，整合 MAF 與 Claude SDK，提供統一的編排體驗。

**HybridOrchestratorV2** (`orchestrator_v2.py`, 1,254 LOC):

```
HybridOrchestratorV2
├── imports Phase 28/29 components (InputGateway, IntentRouter, etc.)
├── FrameworkSelector → 選擇 MAF / Claude SDK / Swarm
├── ContextBridge → 兩個框架間的上下文同步
├── UnifiedToolExecutor → 統一工具路由
├── CheckpointManager → 狀態持久化
├── RiskEngine → 風險評分
└── SwitchingLogic → 框架動態切換
```

**子模組詳情**:

| 子模組 | 檔案數 | 核心職責 |
|--------|--------|----------|
| intent/ | 7 | FrameworkSelector, 分類器, 分析器 |
| context/ | 10 | ContextBridge, 同步, 映射 |
| execution/ | 5 | UnifiedToolExecutor, 工具路由 |
| risk/ | 8 | Risk Engine, 評分器, 分析器 |
| switching/ | 9 | 框架切換 (user/failure/resource/complexity triggers) |
| checkpoint/ | 9 | 4 種儲存後端 (memory/redis/postgres/filesystem) |

### 4.6 Layer 6: MAF Builder Layer (53 files, ~37,209 LOC)

此層是本平台最成熟的整合層。22 個 Builder 檔案均使用官方 `from agent_framework import` 語句，建立真正的 MAF 實例。

**Builder 清單與用途**:

| Builder | 檔案 | 用途 | API 驗證 |
|---------|------|------|----------|
| Handoff | `handoff.py` | Agent 間任務交接 | ✅ `from agent_framework` |
| GroupChat | `groupchat.py` | 多 Agent 群組討論 | ✅ `from agent_framework` |
| Concurrent | `concurrent.py` | 並行 Agent 執行 | ✅ `from agent_framework` |
| Magentic | `magentic.py` | 推理鏈 Agent | ✅ `from agent_framework` |
| Planning | `planning.py` | 任務規劃 Agent | ✅ `from agent_framework` |
| Nested Workflow | `nested_workflow.py` | 巢狀工作流 | ✅ `from agent_framework` |
| Workflow Executor | `workflow_executor.py` | 工作流執行器 | ✅ `from agent_framework` |
| Agent Executor | `agent_executor.py` | Agent 執行器 | ✅ `from agent_framework` |
| Code Interpreter | `code_interpreter.py` | 代碼解釋器 | ✅ `from agent_framework` |

**支援檔案** (均位於 `agent_framework/builders/`):
- `handoff_hitl.py` (1,005 LOC) - HITL 整合的 Handoff
- `handoff_policy.py` (513 LOC) - Handoff 策略管理
- `handoff_capability.py` (1,050 LOC) - 能力匹配
- `handoff_context.py` (855 LOC) - 上下文傳遞
- `handoff_service.py` (821 LOC) - Handoff 服務層
- `groupchat_voting.py` (736 LOC) - GroupChat 投票機制
- `groupchat_orchestrator.py` (883 LOC) - GroupChat 編排
- `edge_routing.py` (884 LOC) - 邊緣路由
- `*_migration.py` (5 files) - 遷移適配器
- `__init__.py` (805 LOC) - 統一重新匯出

**驗證方式**:

```bash
cd backend && python scripts/verify_official_api_usage.py
# All 5 checks must pass
```

### 4.7 Layer 7: Claude SDK Layer (46 files, ~15,098 LOC)

Claude SDK 層提供自主執行能力，是平台的「智慧引擎」。

**核心客戶端**:

```python
# integrations/claude_sdk/client.py (171 LOC)
# 使用 AsyncAnthropic - 真正的 API 客戶端
from anthropic import AsyncAnthropic
```

**自主執行管線**:

```
Task Input
    │
    ▼
┌──────────────┐
│   Analyzer   │ ← 分析任務需求和上下文
│  (analyzer.py)│
└──────┬───────┘
       ▼
┌──────────────┐
│   Planner    │ ← 規劃執行步驟
│  (planner.py) │
└──────┬───────┘
       ▼
┌──────────────┐
│  Executor    │ ← 執行計劃 (Agentic Loop)
│ (executor.py) │
└──────┬───────┘
       ▼
┌──────────────┐
│  Verifier    │ ← 驗證執行結果
│ (verifier.py) │
└──────┬───────┘
       │
  ┌────┴────┐
  │ 成功?   │
  └────┬────┘
  Yes  │  No → Retry (retry.py) → Fallback (fallback.py)
       │
       ▼
  Task Complete
```

**Hook 系統** (安全護欄):

| Hook | 用途 | 觸發時機 |
|------|------|----------|
| `approval.py` | 高風險操作前請求人工審批 | 工具調用前 |
| `audit.py` | 記錄所有操作到審計日誌 | 每次操作 |
| `rate_limit.py` | API 調用速率限制 | API 調用前 |
| `sandbox.py` | 沙箱環境隔離 | 代碼執行前 |

### 4.8 Layer 8: MCP Layer (42 files, ~12,535 LOC)

MCP (Model Context Protocol) 層提供統一的工具存取介面，讓 Agent 能安全地操作外部系統。

**5 個 MCP Server**:

| Server | 檔案數 | 功能模組 | 用途 |
|--------|--------|----------|------|
| Azure | 9 | VM, Resource, Monitor, Network, Storage | Azure 雲端資源管理 |
| Shell | 5 | 命令執行 | 本地系統操作 |
| Filesystem | 5 | 檔案操作 | 檔案讀寫 |
| SSH | 5 | 遠端連線 | 遠端系統管理 |
| LDAP | 5 | 目錄查詢 | 使用者/群組管理 |

**安全設計**:

```
Security Module (mcp/security/, 3 files)
├── 28 permission patterns → 細粒度權限控制
├── 16 audit patterns     → 操作審計記錄
└── RBAC integration      → 角色權限映射
```

### 4.9 Layer 9: Supporting Integrations + Swarm (43 files, ~12,592 LOC)

| 模組 | 檔案數 | LOC | 用途 | 狀態 |
|------|--------|-----|------|------|
| **swarm/** | **7** | **~2,747** | **Swarm 追蹤、事件、整合** | **✅ REAL (V3 NEW)** |
| memory/ | 5 | ~1,508 | mem0 客戶端, 向量嵌入, 統一記憶 | ✅ REAL |
| patrol/ | 11 | ~2,541 | 5 種巡檢類型, 排程器, 巡檢 Agent | ✅ REAL |
| llm/ | 7 | ~1,789 | Azure OpenAI 客戶端 (AsyncAzureOpenAI verified) | ✅ REAL |
| audit/ | 4 | ~1,166 | 決策追蹤, 報告 | ✅ REAL |
| correlation/ | 4 | ~1,188 | 圖分析, 關聯識別 | ⚠️ PARTIAL |
| learning/ | 5 | ~1,236 | Few-shot 學習, 案例提取, 相似度匹配 | ⚠️ PARTIAL |
| a2a/ | 4 | ~888 | Agent-to-Agent Protocol | ✅ REAL |

### 4.10 Layer 10: Domain Layer (112 files, ~40,000+ LOC)

業務邏輯層包含 19 個 domain modules，是平台的業務核心。

### 4.11 Layer 11: Infrastructure (~22 files, ~3,101 LOC)

| 組件 | 狀態 | 說明 |
|------|------|------|
| PostgreSQL 16 | ✅ 運行中 | 主資料庫 (port 5432) |
| Redis 7 | ✅ 運行中 | 快取 (port 6379) |
| RabbitMQ | ❌ 空殼 | 僅 `__init__.py`，尚未實現 |
| Storage | ❌ 空目錄 | `infrastructure/storage/` 無任何實現 |

---

## 5. 並行處理架構

### 5.1 各層並行能力現狀

| 層級 | 並行模式 | 現狀 | 限制 |
|------|----------|------|------|
| **Layer 1: Frontend** | React 18 並行渲染 | 支援 | 正常 |
| **Layer 2: API** | FastAPI async | 原生支援 | 單 Uvicorn Worker |
| **Layer 4: Routing** | 瀑布式 (非並行) | 三層依序嘗試 | 設計如此，非問題 |
| **Layer 5: Hybrid** | asyncio 協程 | 支援並行任務分派 | ContextSynchronizer 無鎖 |
| **Layer 6: MAF** | MAF Concurrent Builder | 原生並行支援 | 受 MAF SDK 限制 |
| **Layer 7: Claude SDK** | AsyncAnthropic | 原生 async | 單 Worker 處理序列 |
| **Layer 8: MCP** | 獨立 Server 進程 | 各 Server 獨立 | 無跨 Server 並行控制 |
| **Layer 9: Swarm** | SwarmTracker 管理 | 支援 PARALLEL 模式 | 受限於 Worker Pool 大小 |

### 5.2 已知並行問題

**問題 1: ContextSynchronizer 記憶體狀態 (嚴重度: 高)**

```python
# integrations/hybrid/context/ 的 ContextSynchronizer
# 使用 dict 作為記憶體存儲，無 threading.Lock 或 asyncio.Lock
# 在多 Worker 或高並行場景下有競爭條件風險

# 現況:
state = {}  # In-memory, no locks

# 建議:
# - 添加 asyncio.Lock
# - 或遷移到 Redis (已有 redis_store.py 可復用)
```

**問題 2: 單 Uvicorn Worker (嚴重度: 高)**

```
# 現況:
uvicorn main:app --reload --host 0.0.0.0 --port 8000
# 僅 1 個 Worker + reload=True (開發模式硬編碼)

# 建議 (生產環境):
uvicorn main:app --workers 4 --host 0.0.0.0 --port 8000
# 或使用 Gunicorn:
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker
```

**問題 3: RabbitMQ 未實現 (嚴重度: 高)**

```
# integrations/infrastructure/messaging/
# 僅有 __init__.py，無實際實現
# 影響: 無法使用非同步訊息佇列進行任務分派
```

**問題 4: 18 個 Mock 類在生產路徑 (嚴重度: 高 - V3 升級)**

```
# V2 判定為「低」，V3 升級為「高」
# 原因: Mock 類與生產代碼並置，無條件分離
# 影響: 運行時可能意外使用 Mock 而非真實實現
# 建議: 將 Mock 移至 tests/mocks/ 或使用工廠模式分離
```

### 5.3 並行處理改善規劃

```
Phase 目標                        優先級    依賴
──────────────────────────────────────────────────
1. ContextSynchronizer + Lock     高        無
2. Multi-Worker Uvicorn           高        無
3. Mock/Production 代碼分離       高        無
4. RabbitMQ 訊息佇列實現          中        Docker
5. Worker Pool 容器化             低        K8s
```

---

## 6. 關鍵設計決策

### 6.1 為什麼是 MAF + Claude SDK 混合而非單一框架

| 考量 | MAF 單獨 | Claude SDK 單獨 | 混合架構 (選擇) |
|------|----------|----------------|-----------------|
| 結構化編排 | ✅ 原生支援 | ❌ 需自建 | ✅ MAF 處理 |
| 自主推理 | ❌ 無 | ✅ Extended Thinking | ✅ Claude 處理 |
| 檢查點 | ✅ 內建 | ❌ 需自建 | ✅ MAF + 4 backends |
| 工具存取 | ⚠️ 有限 | ✅ MCP 原生 | ✅ 統一 MCP |
| 企業治理 | ✅ 審計追蹤 | ⚠️ 需 Hooks | ✅ 兩者結合 |
| 動態切換 | ❌ | ❌ | ✅ SwitchingLogic |

### 6.2 三層意圖路由的設計理由

```
為什麼不直接用 LLM 分類所有意圖?

成本與延遲考量:
────────────────
PatternMatcher:  < 10ms,   $0 (規則匹配)     → 處理 60% 常見請求
SemanticRouter:  < 100ms,  $0 (向量計算)     → 處理 25% 語義請求
LLMClassifier:   < 2000ms, $$ (API 調用)     → 處理 15% 複雜請求

結果: ~85% 請求不需要 LLM 調用
      平均延遲從 ~2000ms 降至 ~50ms
      API 成本降低 ~85%
```

### 6.3 Checkpoint 4 後端設計

```
為什麼需要 4 種 Checkpoint 儲存後端?

Memory:     開發和測試時使用 (快速, 無持久化)
Redis:      生產推薦 (快速, TTL, 適合熱數據)
PostgreSQL: 合規要求 (持久化, 可查詢, 審計)
Filesystem: 備用方案 (簡單, 無依賴)

根據環境自動選擇，支持運行時切換。
```

### 6.4 AG-UI SSE vs WebSocket

```
為什麼選擇 SSE 而非 WebSocket?

1. Agent 通信是單向的: Agent → 前端 (狀態更新)
2. 前端 → Agent 的指令走標準 HTTP API
3. SSE 更簡單、自動重連、防火牆友好
4. 與 FastAPI 原生整合更順暢
5. 不需要雙向即時通信的場景

但保留 WebSocket 作為未來選項 (如即時協作編輯)。
```

### 6.5 MCP 而非直接 API 整合

```
為什麼用 MCP Server 包裝工具而非直接 API 調用?

1. 統一介面: 所有工具遵循相同的 Protocol
2. 安全隔離: 每個 Server 獨立進程，隔離故障
3. 權限統一: 28 種 permission patterns 統一管理
4. 審計統一: 16 種 audit patterns 統一記錄
5. Agent 無關: MAF 和 Claude SDK 都能使用同一套工具
6. 可擴展: 新增工具只需新增 MCP Server
```

### 6.6 Swarm SSE 事件設計 (V3 NEW)

```
為什麼 Swarm 事件透過 AG-UI CustomEvent 而非獨立通道?

1. 複用現有 SSE 基礎設施: AG-UI Bridge 已有成熟的 SSE 連線管理
2. 統一事件模型: 所有 Agent 事件 (含 Swarm) 走同一通道
3. 前端簡化: 不需要管理多個 SSE 連線
4. 向後相容: CustomEvent 是 AG-UI 標準擴展機制

Swarm 9 種事件透過 SwarmIntegration 橋接:
├── SwarmIntegration.bridge_event() → AG-UI CustomEvent
├── 前端透過 AG-UI event handler 接收
└── swarmStore 解析 CustomEvent.payload 更新狀態

但保留獨立 SSE 端點 (/api/v1/swarm/demo/events/{swarm_id})
作為 Demo 模式的直接串流通道。
```

---

## 7. 可觀測性設計

### 7.1 指標體系

```python
# integrations/orchestration/metrics.py (893 LOC) ← V3 更新

OrchestrationMetrics:
├── Routing Metrics
│   ├── 各層命中率 (pattern/semantic/llm)
│   ├── 各層延遲分布
│   └── 路由決策信心度分布
├── Dialog Metrics
│   ├── 對話輪次分布
│   ├── 資訊完成率
│   └── 用戶放棄率
├── Risk Metrics
│   ├── 風險等級分布 (LOW/MEDIUM/HIGH/CRITICAL)
│   └── 審批通過率
├── HITL Metrics
│   ├── 審批請求量
│   ├── 審批等待時間
│   └── 超時升級率
└── Execution Metrics
    ├── 框架選擇分布 (MAF vs Claude vs Swarm)
    ├── 任務完成率
    └── 執行時間分布
```

### 7.2 OpenTelemetry 整合

```
OrchestrationMetrics 設計:
─────────────────────────
• 優先使用 OpenTelemetry (如果可用)
• 自動降級到 fallback counters (dict-based)
• 零配置啟動，不因 OTel 不可用而失敗
• 生產環境接入 Azure Monitor / Grafana
```

### 7.3 巡檢系統

```python
# integrations/patrol/ (11 files, ~2,541 LOC) ← V3 更新

Patrol Agent:
├── 5 種巡檢類型
│   ├── Health Check     → 服務健康狀態
│   ├── Performance      → 效能指標
│   ├── Security         → 安全合規
│   ├── Resource         → 資源使用率
│   └── Configuration    → 配置一致性
├── Scheduler            → 定時巡檢排程
└── Agent                → 自主巡檢 Agent
```

### 7.4 審計追蹤

```
Decision Audit Trail (每次 Agent 決策記錄):
─────────────────────────────────────────
# integrations/audit/ (4 files, ~1,166 LOC) ← V3 更新

1. Intent Classification → 為什麼歸類為此意圖
2. Risk Assessment       → 為什麼判定此風險等級
3. Framework Selection   → 為什麼選擇 MAF/Claude/Swarm
4. Tool Invocations      → 調用了哪些工具，結果如何
5. HITL Decisions        → 人工審批的決定和理由
6. Execution Result      → 最終結果和驗證
```

---

## 8. 總結與展望

### 8.1 平台定位總結

IPA Platform 是一個**智能體編排平台**，其核心定位：

1. **不是 n8n 的替代品** --- 確定性工作流由 n8n/Power Automate 處理
2. **處理不確定性任務** --- 需要 AI 判斷力、推理能力、人機互動的複雜場景
3. **MAF + Claude SDK 混合** --- 結構化治理 + 自主智慧，各取所長
4. **企業級治理** --- 完整審計、HITL 審批、風險評估、可觀測性
5. **統一工具存取** --- MCP Protocol 提供安全、統一的外部系統操作
6. **Swarm 群集可視化** --- 多 Agent 協作的即時追蹤與狀態串流 (V3 NEW)

### 8.2 實現成熟度

```
成熟度評估 (基於代碼量和完成度, V3 驗證):

Layer 1:  Frontend        ████████████████████ 92% (~180 .tsx/.ts, 39 pages, 102+ comp)
Layer 2:  API             ████████████████████ 95% (~534 endpoints, 41 routers)
Layer 3:  AG-UI           ████████████████████ 85% (~9.5K LOC, SSE + Swarm events)
Layer 4:  Orchestration   ████████████████████ 92% (~15.8K LOC, Phase 28 + 真實 LLM)
Layer 5:  Hybrid          ████████████████░░░░ 85% (~21.2K LOC, 59 files)
Layer 6:  MAF Builder     ████████████████████ 95% (~37.2K LOC, 22 builders)
Layer 7:  Claude SDK      ████████████████░░░░ 82% (~15.1K LOC, 46 files)
Layer 8:  MCP             ████████████████░░░░ 80% (~12.5K LOC, 5 servers)
Layer 9:  Supporting      ████████████████░░░░ 75% (+Swarm ✅, learning ⚠️)
Layer 10: Domain          ████████████████████ 90% (~40K LOC, 112 files)
Layer 11: Infrastructure  ████████░░░░░░░░░░░░ 45% (RabbitMQ/Storage empty)
```

### 8.3 後續規劃重點

| 優先級 | 項目 | 原因 |
|--------|------|------|
| **P0** | ContextSynchronizer 並行安全 | 生產環境必要 |
| **P0** | Multi-Worker Uvicorn | 生產環境效能 |
| **P0** | Mock/Production 代碼分離 | 運行時安全 |
| **P1** | RabbitMQ 訊息佇列實現 | 非同步任務分派 |
| **P1** | InMemory* 存儲替換 | 數據持久化 |
| **P1** | Learning 模組完善 | 平台學習能力 |
| **P1** | ReactFlow 整合 (WorkflowViz) | 工作流視覺化 |
| **P2** | Worker Pool 容器化 | 擴展性 |
| **P2** | console.log 清理 | 前端效能與安全 |
| **P2** | 超長檔案拆分 (>800 行) | 可維護性 |
| **P3** | 更多 MCP Server | 工具生態擴展 |
| **P3** | Stale TODO 清理 | 代碼衛生 |

### 8.4 架構演進方向

```
V1 → V2 → V3 → V4 演進:
═══════════════════════

V1 (Phase 1-14): 企業 IT 事件智能處理平台
    ↓ 重新定位
V2 (Phase 15-28): 智能體編排平台 + 三層路由 + HITL + MCP
    ↓ Swarm 可視化
V3 (Phase 29, 現在): + Agent Swarm + 真實 LLM 路由 + 深度驗證
    ↓ 生產化
V4 目標:
    ├── 生產化 (Multi-Worker, Message Queue, Container)
    ├── Mock 清除 (18 Mock → 0, 6 InMemory → Redis/Postgres)
    ├── 學習閉環 (Few-shot 學習 + 案例庫完善)
    ├── WorkflowViz (ReactFlow 視覺化工作流編輯器)
    ├── 擴展 MCP 生態 (更多 Server: K8s, Database, etc.)
    └── Swarm 增強 (真實 Agent 執行 + 動態 Worker 分配)
```

---

## 更新歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| 3.0 | 2026-02-09 | V3 全面更新: Phase 29 Agent Swarm Visualization 完成；新增 Section 3.6 Swarm 能力；所有層級數據由 5 個分析 Agent 並行深度驗證更新；已知問題從 5 項擴展至 11 項 (含嚴重度修正)；CHANGE-003 SemanticRouter/LLMClassifier 真實 LLM 支援；CHANGE-004 Swarm Demo API SSE 串流 |
| 2.1 | 2026-01-28 | 架構層級命名統一：將 Layer A/B 改為 L1-L11 標準編號；補齊 L10 Domain Layer 和 L11 Infrastructure 標籤；Section 4.x 按 L1→L11 順序重排；Section 5.1 和 8.2 補齊缺失層級標號 |
| 2.0 | 2026-01-28 | 全面重寫：重新定位為 Agent Orchestration Platform；更新所有層級的實際 LOC 和檔案數；新增平台定位、核心能力矩陣、並行處理分析；基於代碼庫調查更新實現狀態 |
| 1.3 | 2026-01-16 | Phase 28 三層意圖路由系統更新 |
| 1.2 | 2026-01-10 | Phase 20 AG-UI 整合更新 |
| 1.1 | 2025-12-20 | Phase 15 Claude SDK 整合 |
| 1.0 | 2025-12-01 | 初始版本 - 企業 IT 事件智能處理平台 |
