# ============================================
# IPA Platform - Environment Variables Template
# ============================================
# 複製此文件為 .env 並填入實際值
# 注意: .env 文件已在 .gitignore 中，不會被提交
#
# 使用方式:
#   cp .env.example .env
#   # 編輯 .env 填入實際值

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
DB_NAME=ipa_platform
DB_USER=ipa_user
DB_PASSWORD=ipa_password
DB_HOST=localhost
DB_PORT=5432

# ============================================
# Redis Cache Configuration
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# ============================================
# Message Queue Configuration
# ============================================
# 本地開發: 使用 RabbitMQ (推薦，零成本)
# 生產環境: 使用 Azure Service Bus

# RabbitMQ (本地開發)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Azure Service Bus (生產環境 - 可選)
# AZURE_SERVICE_BUS_CONNECTION_STRING=

# ============================================
# Azure OpenAI Configuration (Required)
# ============================================
# Microsoft Agent Framework 需要 Azure OpenAI
# 從 Azure Portal > Azure OpenAI > Keys and Endpoint 獲取
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================
# Application Settings
# ============================================
APP_ENV=development
LOG_LEVEL=DEBUG
SECRET_KEY=change-this-to-a-secure-random-string

# CORS Settings (允許的前端來源)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================
# JWT Authentication
# ============================================
JWT_SECRET_KEY=change-this-to-a-secure-random-string
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ============================================
# Monitoring (Optional)
# ============================================
# 使用 docker-compose --profile monitoring up -d 啟用

# Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# OpenTelemetry
OTEL_ENABLED=false
OTEL_SERVICE_NAME=ipa-platform
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# ============================================
# Feature Flags
# ============================================
FEATURE_WORKFLOW_ENABLED=true
FEATURE_AGENT_ENABLED=true
FEATURE_CHECKPOINT_ENABLED=true

# ============================================
# Development Settings
# ============================================
# 是否啟用 API 文檔 (Swagger UI)
ENABLE_API_DOCS=true

# 是否啟用 SQL 查詢日誌
ENABLE_SQL_LOGGING=false
