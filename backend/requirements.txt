# IPA Platform V2 — Backend runtime dependencies
#
# Locked versions for reproducible installs.
# DEV dependencies live in pyproject.toml [project.optional-dependencies].
#
# DELIBERATELY EXCLUDED in Sprint 49.1 (added in later sprints):
#   - openai / anthropic SDKs       → Sprint 49.3 adapters
#   - agent-framework (MAF)         → Sprint 54.2 (if needed; archived V1 had it)
#   - sentence-transformers etc.    → Sprint 51.2 (memory layer)
#
# Per LLM-provider-neutrality rule: agent_harness/** must never import
# LLM SDKs directly. Only adapters/<provider>/ may.

# ---- Web framework ---------------------------------------------------
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.35
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# ---- Database --------------------------------------------------------
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
alembic>=1.13,<2.0

# ---- Cache / messaging -----------------------------------------------
redis>=5.0,<6.0
# Sprint 52.5 Day 10 P1 #6 (W2-2): Celery removed; Phase 49.4 worker-queue
# decision selected Temporal (workflow-native + replay durability) over Celery.
# `temporalio>=1.6,<2.0` joins this file in Phase 53.1 when TemporalQueueBackend
# lands per docs/03-implementation/agent-harness-planning/06-phase-roadmap.md.

# ---- Sandbox isolation (Sprint 52.5 P0 #17) --------------------------
# Docker SDK for DockerSandbox — replaces best-effort SubprocessSandbox
# (decorative on Windows: resource.setrlimit POSIX-only; W4P-3 audit
# proved os.listdir("C:/") leaked host fs from inside the "sandbox").
# Real isolation via short-lived Docker containers with --memory --cpus
# --network=none --read-only --cap-drop=ALL.
docker>=7.0,<8.0

# ---- Observability (Sprint 49.4 wires up the 11+1 categories) --------
# OTLP exporters preferred over deprecated jaeger-exporter; modern Jaeger
# 1.55+ accepts OTLP/gRPC natively on port 4317.
# Sprint 52.5 P0 #15 (issue #15): version-pinned per
# .claude/rules/observability-instrumentation.md to avoid breaking changes.
# Auditor evidence: W4P-1 found floating range `>=1.27,<2.0` violates rule.
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp==1.22.0
opentelemetry-exporter-prometheus==0.43b0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-instrumentation-sqlalchemy==0.43b0
opentelemetry-instrumentation-redis==0.43b0
python-json-logger>=2.0,<4.0

# ---- Identity / Auth (Sprint 52.5 P0 #14) ----------------------------
# JWT replaces V1 X-Tenant-Id header (W1-2 audit P0). HS256 dev / RS256 prod;
# `cryptography` extras include backends needed for RS256.
# See platform_layer/identity/jwt.py + tenant_context.py.
python-jose[cryptography]>=3.3,<4.0

# ---- Sprint 52.6 US-1: prod deps missed by 49.3+ adapter sprint ------
# AD-5 (V2-AUDIT-OPEN-ISSUES-20260501) — CI mypy was failing for these
# import-not-found errors since adapter / business_domain code shipped.
# Sprint 49.1's "DELIBERATELY EXCLUDED" comment for openai was meant to
# be cleared in Sprint 49.3 but was not. Adding now.
httpx>=0.27,<1.0           # business_domain/*/mock_executor.py (5 modules)
tiktoken>=0.7,<1.0          # context_mgmt/token_counter/tiktoken_counter.py + adapters/azure_openai
requests>=2.31,<3.0         # agent_harness/tools/sandbox.py
openai>=1.40,<2.0           # adapters/azure_openai/error_mapper.py (LLM-neutral allowlist: only adapters/<provider>/)
jsonschema>=4.20,<5.0       # agent_harness/tools/{registry,executor}.py — input_schema validation (51.1)

# ---- Sprint 57.6 US-2: .env autoload (closes AD-Reality-2 + 57.5 D-20) -
# Loaded by api/main.py _lifespan() so AZURE_OPENAI_API_KEY etc. populate
# os.environ before settings / adapter initialization on uvicorn startup.
python-dotenv>=1.0,<2.0

# ---- Sprint 57.7 US-A2: IAM Hosted vendor (WorkOS chosen per US-A1 matrix) -
# Vendor SDK consumed only from platform_layer/identity/oidc.py;
# LLM-neutral allowlist N/A (workos is auth SDK, not LLM SDK).
# Path 1 per Day 0 D2 verify: vendor SDK handles JWKS validation of
# WorkOS-issued tokens; V2 internal JWT remains HS256 via JWTManager.
workos>=4.0,<6.0

# ---- Sprint 57.86 US-1: local-password credentials (C-12 IAM Block B/C) -----
# bcrypt (used directly — no passlib middle layer, avoids passlib's bcrypt-4.x
# compat noise) hashes local passwords set at invite-accept + verified by
# POST /auth/password-login. Consumed only from platform_layer/identity/
# passwords.py (sync hash/verify offloaded via anyio.to_thread.run_sync).
bcrypt>=4.1,<5.0

# ---- Sprint 57.112 US-1: TOTP second factor (C-12 IAM Block C MFA) ----------
# pyotp implements RFC 6238 TOTP (stdlib-hmac, no native deps). Consumed only
# from platform_layer/identity/mfa.py (TOTPService.enroll/confirm/verify) +
# the drive-through helper. random_base32() generates the shared secret;
# TOTP(secret).verify(code, valid_window=1) absorbs ±30s skew; provisioning_uri()
# builds the otpauth:// URI the authenticator app scans.
pyotp>=2.9,<3.0

# ---- Sprint 57.113 US-1: Skills System (model-invoked lazy-load) ------------
# PyYAML parses the YAML frontmatter (name + description) of bundled SKILL.md
# files in agent_harness/skills/registry.py::SkillRegistry.from_dir. It was
# already imported transitively (guardrails/tool/capability_matrix.py) but
# never declared — declared now (the from_dir loader depends on it directly).
PyYAML>=6.0,<7.0

# ---- Sprint 57.146 US-3: knowledge embedding/Qdrant vector search -----------
# qdrant-client wraps the dev Qdrant container (docker-compose.dev.yml:74) for the
# knowledge vector index (infrastructure/vector/qdrant_client.py). >=1.12 gives the
# query_points API (the deprecated `search` was removed by 1.17). Closes the
# CARRY-026 Qdrant-client deferral for the KB use case. Embeddings reuse the openai
# SDK (already declared) via adapters/azure_openai/embeddings.py — no new LLM dep.
qdrant-client>=1.12,<2.0
