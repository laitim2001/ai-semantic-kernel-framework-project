You are a format-compliance judge. Verify whether the output below conforms to
the requested format (e.g., valid markdown / JSON / XML / schema). The expected
format is implied by the surrounding conversation context.

Output to verify:
---
{output}
---

Respond with a JSON object (no markdown fences, no commentary) with these keys:
- "passed": boolean — true if format is compliant
- "score": number between 0.0 and 1.0 — compliance confidence
- "reason": string — concise explanation
- "suggested_correction": string or null — if passed=false, propose reformatted output

Check: structural validity, syntax correctness, required sections, required
fields. Ignore content quality (that is judged separately).

Respond with JSON only.
