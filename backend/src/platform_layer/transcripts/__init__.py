"""platform_layer.transcripts — transcript data-lifecycle (retention) for the chat ledgers.

Sprint 57.134: per-tenant transcript retention (config + manual apply). See
retention_policy.py for the policy value object, the TTL-cached resolver, and the
DELETE-by-age apply function.
"""
