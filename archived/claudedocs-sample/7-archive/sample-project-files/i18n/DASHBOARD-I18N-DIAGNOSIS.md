# Dashboard i18n 診斷報告

> **生成日期**: 2025-11-03 22:15
> **問題嚴重性**: P0 (阻塞)
> **影響範圍**: Dashboard 頁面無法載入

---

## 📋 問題摘要

Dashboard 頁面的翻譯結構與代碼使用方式不匹配，導致運行時錯誤。

### 錯誤類型
1. **INSUFFICIENT_PATH** - 嘗試直接使用物件作為字串
2. **MISSING_MESSAGE** - 缺少必要的翻譯 key

---

## 🔍 詳細分析

### 問題 1: `dashboard.stats.monthlyBudget` 結構錯誤

**代碼使用** (page.tsx:30):
```typescript
title: t('stats.monthlyBudget'),  // 期望得到字串
```

**實際 JSON 結構** (zh-TW.json):
```json
"stats": {
  "monthlyBudget": {
    "title": "本月預算額",
    "change": "較上月"
  }
}
```

**問題**: `dashboard.stats.monthlyBudget` 是一個物件，但代碼期望直接得到字串。

**正確用法應為**:
```typescript
title: t('stats.monthlyBudget.title'),
```

---

### 問題 2: `dashboard.stats.activeProjects` 結構錯誤

**代碼使用** (page.tsx:36):
```typescript
title: t('stats.activeProjects'),  // 期望得到字串
```

**實際 JSON 結構**:
```json
"stats": {
  "activeProjects": {
    "title": "進行中項目",
    "change": "較上月"
  }
}
```

**問題**: 同樣是物件 vs 字串的問題。

---

### 問題 3: `dashboard.stats.vsLastMonth` 缺失

**代碼使用** (page.tsx:32, 38, 44, 50):
```typescript
change: { value: '+12.5%', isPositive: true, label: t('stats.vsLastMonth') },
```

**實際 JSON 結構**:
```json
"stats": {
  "monthlyBudget": {
    "title": "本月預算額",
    "change": "較上月"  // ← 這是巢狀在每個 stat 下
  }
}
```

**問題**: `dashboard.stats.vsLastMonth` 這個 key 根本不存在。應該是 `stats.monthlyBudget.change`，但每個 stat 都有自己的 change。

---

### 問題 4: 其他類似結構問題

**所有受影響的 stats**:
1. `stats.monthlyBudget` - 物件 vs 字串
2. `stats.activeProjects` - 物件 vs 字串
3. `stats.pendingProposals` - 物件 vs 字串 (推測，未見完整 JSON)
4. `stats.budgetUtilization` - 物件 vs 字串 (推測)

---

## 🎯 修復方案

### 方案 A: 調整 JSON 結構 (推薦)

**優點**:
- 代碼改動最少
- 符合當前代碼邏輯
- 更扁平的結構，更容易維護

**缺點**:
- 需要重新組織 JSON

**修改後的 JSON 結構**:
```json
"dashboard": {
  "title": "儀表板",
  "welcome": "歡迎回來！查看您的專案進度和最新動態",
  "stats": {
    "monthlyBudget": "本月預算額",
    "activeProjects": "進行中項目",
    "pendingProposals": "待審批提案",
    "budgetUtilization": "預算使用率",
    "vsLastMonth": "較上月"
  },
  "budgetTrend": { ... },
  "quickActions": { ... },
  "recentActivities": { ... },
  "aiInsights": { ... }
}
```

---

### 方案 B: 調整代碼使用方式

**優點**:
- 保留更結構化的 JSON
- 更容易理解翻譯的組織

**缺點**:
- 需要修改多處代碼
- 代碼變得更冗長

**修改後的代碼**:
```typescript
const stats = [
  {
    title: t('stats.monthlyBudget.title'),
    value: 'RM 485,200',
    change: {
      value: '+12.5%',
      isPositive: true,
      label: t('stats.monthlyBudget.change')  // 每個 stat 都要單獨指定
    },
    icon: Wallet,
  },
  // ...
];
```

---

## ✅ 建議執行方案

**採用方案 A** - 調整 JSON 結構

### 理由:
1. 代碼已經按照扁平結構設計
2. 所有 stats 共用同一個 "較上月" 文字，不需要重複
3. 改動最小，風險最低
4. 更符合 DRY 原則

### 實施步驟:
1. 修改 `zh-TW.json` 的 `dashboard.stats` 結構
2. 修改 `en.json` 的 `dashboard.stats` 結構
3. 驗證 JSON 語法正確性
4. 清除 .next 目錄
5. 重啟服務器
6. 測試 Dashboard 頁面載入

---

## 🔄 預防措施

### 已識別的風險:
1. **結構不一致** - 不同頁面的翻譯結構設計不統一
2. **缺乏驗證** - 沒有工具檢查 JSON 結構與代碼使用是否匹配
3. **測試不足** - 頁面標記為完成但未經過實際執行測試

### 建議改進:
1. **創建 TypeScript 類型定義** - 為翻譯 JSON 創建嚴格的類型定義
2. **編寫驗證腳本** - 檢查所有翻譯 key 是否存在且類型正確
3. **強制執行測試** - 每個頁面遷移後必須在瀏覽器中載入驗證
4. **統一設計模式** - 制定翻譯結構的標準設計模式

---

## 📊 影響評估

### 已知受影響文件:
- ✅ `apps/web/src/app/[locale]/dashboard/page.tsx` - 需要 JSON 修復

### 潛在受影響文件 (需要檢查):
- ⚠️ 所有 Batch 1 完成的頁面 (9 個文件)
- ⚠️ 所有 Batch 2 完成的頁面 (11 個文件)
- ⚠️ 所有 Batch 3 完成的頁面 (24 個文件)

### 檢查方法:
```bash
# 檢查所有使用 useTranslations 的文件
grep -r "useTranslations" apps/web/src/app

# 啟動服務器並手動訪問每個頁面
PORT=3009 pnpm dev --filter @itpm/web

# 檢查控制台是否有 IntlError
```

---

## 🎯 下一步行動

### 立即行動 (P0):
1. ✅ 生成診斷報告 (本文件)
2. ⏳ 修復 Dashboard 頁面 JSON 結構
3. ⏳ 驗證修復成功

### 短期行動 (P1):
4. ⏳ 系統化檢查所有已完成頁面
5. ⏳ 創建驗證工具腳本
6. ⏳ 建立測試清單

### 長期行動 (P2):
7. ⏳ 創建 TypeScript 類型定義
8. ⏳ 制定翻譯結構標準
9. ⏳ 集成到 CI/CD 流程

---

**維護者**: Development Team + AI Assistant
**最後更新**: 2025-11-03 22:15
**版本**: 1.0
